// Generated by tools/gen_mm_animmap.py -- DO NOT EDIT BY HAND.
// MM3D actor GAR basename -> the CSAB clip to play when the live N64 animation has no
// entry in kMMAnimMaps. See resolve_default_anim() in the generator for the priority and
// for the priority. An actor ABSENT from this table has no determinable idle; it keeps
// the runtime's first-CSAB fallback, which is GAR ordering rather than evidence (it picks
// pr_damage for object_pr). That is a known gap, listed by the generator, not a default.
    { "zelda2_field_keep", "butterfly_fly" },                // the archive's only clip
    { "zelda2_keep", "elf_wait" },                           // GAR *wait* clip
    { "zelda2_ah", "ah_wait" },                              // GAR *wait* clip
    { "zelda2_al", "al_suwaruBwait" },                       // GAR *wait* clip
    { "zelda2_an1", "an_ajimi_wait" },                       // GAR *wait* clip
    { "zelda2_and", "wa_utumuki_loop" },                     // clip mapped from N64 /Idle/ symbol gAndIdleAnim
    { "zelda2_ani", "Ani_wait" },                            // GAR *wait* clip
    { "zelda2_aob", "Aob_wait" },                            // GAR *wait* clip
    { "zelda2_az", "bea_wait" },                             // GAR *wait* clip
    { "zelda2_bai", "bai_wait01" },                          // GAR *wait* clip
    { "zelda2_bal", "bal_wait" },                            // GAR *wait* clip
    { "zelda2_bba", "sb_taorewait" },                        // GAR *wait* clip
    { "zelda2_bee", "bee_die_wait" },                        // GAR *wait* clip
    { "zelda2_bg", "bg2_newwait" },                          // GAR *wait* clip
    { "zelda2_bh", "bh_fly" },                               // the archive's only clip
    { "zelda2_bigokuta", "ocd_wait" },                       // GAR *wait* clip
    { "zelda2_bigpo", "pog_wait" },                          // GAR *wait* clip
    { "zelda2_bigslime", "bsl_climwait" },                   // GAR *wait* clip
    { "zelda2_bji", "Bji_wait01" },                          // GAR *wait* clip
    { "zelda2_bjt", "bjt_wait" },                            // GAR *wait* clip
    { "zelda2_boj", "Boj_card_aka_wait" },                   // GAR *wait* clip
    { "zelda2_boss01", "kyoro_wait" },                       // GAR *wait* clip
    { "zelda2_boss03", "bus_base" },                         // clip mapped from N64 /Idle/ symbol gGyorgIdleAnim
    { "zelda2_boss04", "wart" },                             // the archive's only clip
    { "zelda2_boss05", "wdb_waitH" },                        // GAR *wait* clip
    { "zelda2_boss07", "last_stop" },                        // clip mapped from N64 /Idle/ symbol gMajorasWrathIdleAnim
    { "zelda2_bsb", "bsb_demo_wait" },                       // GAR *wait* clip
    { "zelda_cow", "ust_daran" },                            // clip mapped from N64 /Idle/ symbol gCowTailIdleAnim
    { "zelda2_cs", "csn_ed_wait" },                          // GAR *wait* clip
    { "zelda2_dai", "dg_iwait" },                            // GAR *wait* clip
    { "zelda_daiku", "dk2_kowasu2wait" },                    // GAR *wait* clip
    { "zelda2_death", "d13_deadwait" },                      // GAR *wait* clip
    { "zelda2_dekunuts", "dn_demowait" },                    // GAR *wait* clip
    { "zelda2_delf", "chat_bikkuri_wait" },                  // GAR *wait* clip
    { "zelda2_dinofos", "zf2_matsu" },                       // clip mapped from N64 /Idle/ symbol gDinolfosIdleAnim
    { "zelda2_dnj", "dnj_douzoTOwaitR" },                    // GAR *wait* clip
    { "zelda2_dnk", "dnk_wait" },                            // GAR *wait* clip
    { "zelda2_dno", "dno_ohwait" },                          // GAR *wait* clip
    { "zelda2_dnq", "dnq_awateTOwait" },                     // GAR *wait* clip
    { "zelda2_dns", "dns_wait" },                            // GAR *wait* clip
    { "zelda2_dnt", "dnt_onedariTOwait" },                   // GAR *wait* clip
    { "zelda_dodongo", "da_dmg_wait" },                      // GAR *wait* clip
    { "zelda2_dog", "dog_wait" },                            // GAR *wait* clip
    { "zelda2_drs", "drs_kihon" },                           // clip mapped from N64 /Idle/ symbol gWeddingDressMannequinIdleAnim
    { "zelda2_ds2n", "ds2n_wait" },                          // GAR *wait* clip
    { "zelda2_dt", "dt_wait" },                              // GAR *wait* clip
    { "zelda_efc_tw", "top_fc_mdl" },                        // the archive's only clip
    { "zelda2_eg", "eg_mahiwait" },                          // GAR *wait* clip
    { "zelda2_famos", "fam_dosun" },                         // clip mapped from N64 /Idle/ symbol gFamosIdleAnim
    { "zelda2_fr", "wait" },                                 // GAR *wait* clip
    { "zelda2_fsn", "fsn_dekuwait01" },                      // GAR *wait* clip
    { "zelda2_ge1", "gb_event_wait" },                       // GAR *wait* clip
    { "zelda2_gg", "gg_wait" },                              // GAR *wait* clip
    { "zelda2_gi_reserve00", "zelda_gi_moontears01" },       // the archive's only clip
    { "zelda2_giant", "kyo_wait" },                          // GAR *wait* clip
    { "zelda2_gk", "gk_stand_wait" },                        // GAR *wait* clip
    { "zelda2_gla", "geldA_event_wait" },                    // GAR *wait* clip
    { "zelda2_gmo", "gm_default" },                          // the archive's only clip
    { "zelda2_ha", "ha_wait" },                              // GAR *wait* clip
    { "zelda2_harfgibud", "hg_wait" },                       // GAR *wait* clip
    { "zelda2_hata", "ht_hata" },                            // the archive's only clip
    { "zelda_hintnuts", "dnh_wait" },                        // GAR *wait* clip
    { "zelda_hs", "hs_matsu" },                              // clip mapped from N64 /Idle/ symbol gHsIdleAnim
    { "zelda2_ikn_demo", "osk_hanasu_wait" },                // GAR *wait* clip
    { "zelda2_in", "ingo_anim_wait1" },                      // GAR *wait* clip
    { "zelda2_in2", "in2_suwaruwait" },                      // GAR *wait* clip
    { "zelda2_jg", "jg_wait" },                              // GAR *wait* clip
    { "zelda2_js", "js_chigau2wait" },                       // GAR *wait* clip
    { "zelda2_jso", "jso_attack_wait" },                     // GAR *wait* clip
    { "zelda_ka", "ka_newwait" },                            // GAR *wait* clip
    { "zelda2_kamejima", "kam_wait" },                       // GAR *wait* clip
    { "zelda2_kbt", "kbt_okiruwait" },                       // GAR *wait* clip
    { "zelda2_kgy", "kgy_en_wait" },                         // GAR *wait* clip
    { "zelda2_kitan", "kt_wait" },                           // GAR *wait* clip
    { "zelda2_knight", "nsk_king_aramaD_wait" },             // GAR *wait* clip
    { "zelda2_kz", "kz_downwait" },                          // GAR *wait* clip
    { "zelda2_link_goron_new", "pg_gurdmotion" },            // the archive's only clip
    { "zelda2_ma1", "rm_wait" },                             // GAR *wait* clip
    { "zelda2_ma2", "kl_wait" },                             // GAR *wait* clip
    { "zelda_mastergolon", "oF1d_omise" },                   // the archive's only clip
    { "zelda_masterzoora", "zo_omise" },                     // the archive's only clip
    { "zelda2_mk", "mk_matsu" },                             // clip mapped from N64 /Idle/ symbol gMarineResearcherIdleAnim
    { "zelda2_mm", "mm_postin_wait" },                       // GAR *wait* clip
    { "zelda2_mnk", "mnk_hashira_tiedwait" },                // GAR *wait* clip
    { "zelda_ms", "ms_matsu" },                              // the archive's only clip
    { "zelda2_mu", "tg_matsu" },                             // clip mapped from N64 /Idle/ symbol gHoneyAndDarlingIdleAnim
    { "zelda2_nb", "nb_wait01" },                            // GAR *wait* clip
    { "zelda2_nwc", "nwc_wait" },                            // GAR *wait* clip
    { "zelda2_ob", "mjk_suwari2wait" },                      // GAR *wait* clip
    { "zelda2_osn", "osn_wait" },                            // GAR *wait* clip
    { "zelda2_ot", "ot_wait" },                              // GAR *wait* clip
    { "zelda2_owl", "owl_wait" },                            // GAR *wait* clip
    { "zelda2_pamera", "pam_wait" },                         // GAR *wait* clip
    { "zelda_po", "po_wait" },                               // GAR *wait* clip
    { "zelda2_po_composer", "pom_wait" },                    // GAR *wait* clip
    { "zelda2_po_fusen", "bal_kihon" },                      // the archive's only clip
    { "zelda2_po_sisters", "pob_wait" },                     // GAR *wait* clip
    { "zelda2_pp", "pp_wait" },                              // GAR *wait* clip
    { "zelda2_pst", "pst_model" },                           // the archive's only clip
    { "zelda2_rd", "re_dead_wait" },                         // GAR *wait* clip
    { "zelda2_rsn", "rsn_wait" },                            // GAR *wait* clip
    { "zelda2_rz", "link_normal_wait_free" },                // GAR *wait* clip
    { "zelda2_sdn", "sdn_hekomiwait" },                      // GAR *wait* clip
    { "zelda2_shn", "shn_wait" },                            // GAR *wait* clip
    { "zelda2_skb", "babestl_keirei_wait" },                 // GAR *wait* clip
    { "zelda2_snowman", "sm_wait" },                         // GAR *wait* clip
    { "zelda2_stk", "kubi_katakata_wait" },                  // GAR *wait* clip
    { "zelda2_tab", "tab_talk02TOwait02" },                  // GAR *wait* clip
    { "zelda2_taisou", "taisou1_osi" },                      // clip mapped from N64 /Stand/ symbol gGoronAthleticsHamstringStretchStandingAnim
    { "zelda2_tl", "tl_wait" },                              // GAR *wait* clip
    { "zelda2_toryo", "dk1_odoroku_wait" },                  // GAR *wait* clip
    { "zelda2_tro", "trO_wait" },                            // GAR *wait* clip
    { "zelda2_trt", "trT_wait" },                            // GAR *wait* clip
    { "zelda2_tru", "trU_flywait" },                         // GAR *wait* clip
    { "zelda2_tsn", "tsn_wait_talk02" },                     // GAR *wait* clip
    { "zelda2_uch", "uch_wait_body" },                       // GAR *wait* clip
    { "zelda2_um", "um_wait" },                              // GAR *wait* clip
    { "zelda2_utubo", "utubo_cave_wait" },                   // GAR *wait* clip
    { "zelda_vm", "vm_akesime" },                            // the archive's only clip
    { "zelda2_wdhand", "wd_waitH" },                         // GAR *wait* clip
    { "zelda2_wf", "wolfman_wait" },                         // GAR *wait* clip
    { "zelda2_wiz", "wiz_wait" },                            // GAR *wait* clip
    { "zelda2_yb", "link_normal_wait_free" },                // GAR *wait* clip
    { "zelda2_zm", "zM_furimuki2wait" },                     // GAR *wait* clip
    { "zelda2_zo", "zo_mizuwait" },                          // GAR *wait* clip
    { "zelda2_zob", "zB_wait01" },                           // GAR *wait* clip
    { "zelda2_zod", "zD_wait01" },                           // GAR *wait* clip
    { "zelda2_zog", "zG_yuurei_talk2wait" },                 // GAR *wait* clip
    { "zelda2_zoraegg", "zb_onpuwait" },                     // GAR *wait* clip
    { "zelda2_zos", "zK_wait01" },                           // GAR *wait* clip
    { "zelda2_zov", "zV_hokan2wait02" },                     // GAR *wait* clip
