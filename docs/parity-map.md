# SoH3D parity map — the CLOSED-CASES registry

The authoritative list of what is **CONFIRMED AT PARITY** with the OoT3D/MM3D oracle. This is the
third orientation map, alongside `docs/codemap.md` (what CODE exists) and `docs/re-frontier.md`
(RE progress / hacks). Consult all three at the START of a task:
**parity-map = skip closed cases · codemap = find the code · re-frontier = next RE step.**

## THE RULE (this map's whole purpose)

**A `CLOSED-parity` item is DONE and is NOT to be re-examined by sweeps, loop iterations, or
autonomous parity passes.** It reopens ONLY on:
1. an explicit **USER request** to reopen the case, or
2. a **confirmed regression** (a matched-frame oracle A/B or a user playtest showing it broke) —
   in which case flip its Status to `REOPENED`, add the regression evidence, and it becomes live
   work again.

This is the mechanism that enforces the global "mark verified, don't revisit" rule
(`<claude-config>` memory `mark-verified-dont-revisit`, `soh3d-parity-meaning`): once a thing is proven
at parity against the oracle, spending further loop budget re-measuring it is waste. Do not
micro-tune a CLOSED lighting/terrain item — several are explicitly **OFF-LIMITS to tuning** by
direct user instruction (`soh3d-stop-microtuning-lighting`, `soh3d-lighting-port`); reopening one
to "improve" it is a rule violation, not initiative.

**How this map relates to the generated checklists:** the Link/MM state rows below are a
CONSOLIDATED, human-authored snapshot. The live, per-run generated detail lives in
`docs/link_parity_checklist.md` (regenerated by `tools/link_sweep.py sweep`) and
`docs/mm_parity_checklist.md` (hand-maintained seed). This map cites their summary + resolving
commits so a session sees "closed, don't re-sweep" without re-running the sweep; the checklists
remain the source of truth for the fine-grained metric of each state. A generator that aggregates
the checklists' MATCH rows into this map's format automatically is a reasonable **follow-up** (see
bottom) — for now this map is hand-authored and authoritative.

Status vocabulary: **CLOSED-parity** (proven at parity, do not revisit) · **OPEN** (not yet at
parity — live work) · **REOPENED** (was closed, regressed — live work again).

---

## CLOSED — parity achieved (do NOT revisit unless user reopens)

### Title cutscene (spot99) — the largest closed arc

| Item | Status | How verified | Verifying commit | Date |
|---|---|---|---|---|
| Title = first-class SCENE_TITLE (spot99), scene-name override retired | CLOSED-parity | RE'd spot99 scene id; oracle scene match | `f4bd51e8` | 2026-07-14 |
| N64-logo boot screen skipped (hardwired, matches OoT3D) | CLOSED-parity | oracle ground truth — OoT3D has no N64 logo | `d938e659` | 2026-07-14 |
| Title camera / spline (LEFT-HANDED basis, FOV 48.803°) | CLOSED-parity | RE'd basis writer (JIT-solved); matched-frame A/B | `f4bd51e8` + title_view_matrix_lh RE | 2026-07-14 |
| Rider (mounted Link intro) cs dispatcher (EnHorse FUN_0026a30c) | CLOSED-parity | RE'd dispatcher: warp-only teleports, last-match cue latch, 0x24=gallop | `0a711e4c` | 2026-07-14 |
| Mounted Link renders as OoT3D model, seated on Epona | CLOSED-parity | oracle A/B — no longer N64/floating | `a318ca18` | 2026-07-15 |
| Epona / En_Horse draws as OoT3D CMB (Zelda3D choke point in Skin_DrawImpl) | CLOSED-parity | oracle A/B render match | `db0696d8` | 2026-07-15 |
| Epona title-cs gait (native CsMove anim funcs, not gameplay approximation) | CLOSED-parity | RE'd CsMove funcs; motion A/B | `4f0b3df6` | 2026-07-15 |
| Epona hoof-dust Y reconciled onto OoT3D-warped terrain (no hill punch-through) | CLOSED-parity | depth/geometry A/B | `91591e26` | 2026-07-14 |
| Wordmark decorations: coordinator-1 CameraSphereEnvMap port (gold outlines visible) | CLOSED-parity | RE'd TEV; root-caused invisible outlines | `efa336cd` | 2026-07-11 |
| Wordmark sphere-map uses CmbVShader c4-c6 normal transform (identity on draws 75-87) + coordinator xform | CLOSED-parity | shader disassembly + cached live uniforms + decoded linear-sample check | current RE correction | 2026-08-30 |
| Wordmark sheen mechanism (shade = 0.18 + max(0, N·(-L(t)))) | CLOSED-parity | RE'd real sheen shader | `2b797541` | 2026-07-10 |
| Fire-glow: real 3-stage TEV combiner (dual-texture ADD_MULT mask, const-palette base) | CLOSED-parity | RE'd combiner; alpha ramp proven EXACT (`65373fa0`) | `9132cdeb` | 2026-07-10 |
| Fire-glow CMAB cursor frame-domain (native 60fps vs half-rate cs-frame) | CLOSED-parity | frame-domain fix, remeasured post-fix | `ab100bb5` | 2026-07-14 |
| Distance fog (3DS PICA fog, RE'd FogResUpdater, predict-gate float-exact) — the dawn-hue root cause | CLOSED-parity | RE'd fog; float-exact port; **already-ported note corrected** (`ac47d649`) | `19081f9a` | 2026-07-11 |
| Death Mountain cloud vortex ACTOR-layer ring (bright x2 half) | CLOSED-parity | RE'd actor-layer ring | `fca48578` | 2026-07-10 |
| Dawn layers attributed (sun-glow dome / kumo vcol-lerp / soft_smoke mist) | CLOSED-parity | RE spec in `oot3d-decomp/docs/title_dawn_layers.md` | `01be570c` | (see doc) |
| Moon halo: mirror-tiled quadrant textures + fixed oracle-anchored scale + per-layer on-screen scale | CLOSED-parity | oracle-anchored; alpha mechanism confirmed correct | `01be79ce` → `3e6186ad` → `3a6e163f` | 2026-07-10/11 |
| Star band: mip-blur footprint + overlay scale from decomp perspective compose | CLOSED-parity | footprint remeasured RESOLVED (`edce4265`) | `9026352c` | 2026-07-10 |
| Sky R/G desync (skyboxTime) + dormant palette-offset bug | CLOSED-parity | live UBO verify | `4f05fc90` | 2026-07-10 |
| Terrain per-enabled-light ambient sum (the ~1.9x terrain brightness root cause) | CLOSED-parity | terrain residual CLOSED, full 10-point sweep (`d3d31335`) | `02181072` | 2026-07-10 |
| Title-cs actors grounded onto OoT3D terrain (tree/hill occlusion) | CLOSED-parity | geometry A/B | `36525326` | 2026-07-15 |
| En_Kusa type-0 → OoT3D field-grass CMB (grass05_model) | CLOSED-parity | asset-id match | `9404c9f5` | 2026-07-14 |
| Composition/skyline offset | CLOSED-parity (EXONERATED) | skyline ≤1px both frames — was a false alarm, not a bug | `2a860af5` | 2026-07-14 |
| Epona mane/tail motion | CLOSED-parity (no fix needed) | already CSAB-driven — investigated, correct as-is | `763d5330` | 2026-07-14 |

### Actor render — the shared render path

| Item | Status | How verified | Verifying commit | Date |
|---|---|---|---|---|
| Skinned-actor choke point (Skin_DrawImpl → Zelda3D CMB) | CLOSED-parity | En_Horse/Epona render proves the path; #109 rigid-skinning + #107 collision closed | `db0696d8` (+ memory #107/#109) | 2026-07-15 |
| CMB rigid skinning (skin_mode 1 per-vertex single-bone) | CLOSED-parity | memory `soh3d-cmb-rigid-skinning` (#109) | — (pre-map) | — |
| Skinned-actor collision limb-walk at DrawSkeletonOpa | CLOSED-parity | memory `soh3d-skinned-actor-collision` (#107) | — (pre-map) | — |
| Field grass (En_Kusa) OoT3D CMB | CLOSED-parity | see title arc `9404c9f5` | `9404c9f5` | 2026-07-14 |
| ~25 named actor behaviors ported (kokiri_kid, saria, malon, mido, townsfolk, ruppy, pot, door*, en_*, boss_goma…) | CLOSED-parity (per-actor) | each close-tested in its porting session; modules under `behaviors/actor/` | (per-actor commits) | various |

### Link on-foot states (from `docs/link_parity_checklist.md` — MATCH=24, DIVERGENT=0, UNREACHABLE=1)

Consolidated; see the generated checklist for per-state metric. Verdict = anim SELECTION unless a
POSE column is noted. **Last swept 2026-07-15T12:36Z.**

| Item | Status | How verified | Verifying commit | Date |
|---|---|---|---|---|
| Link on-foot: 24 of 25 states MATCH (idle, walk, run, backwalk, sidestep_l/r, turn_in_place, jump, roll, attack, attack_combo, shield, item_bottle_use, pickup_carry, throw, climb_hang, climb_updown, swim_surface, swim_dive, mount_dismount, damage_knockback, getitem_pose, death, model_render) | CLOSED-parity | `link_sweep.py sweep` — CSAB-family SELECTION match vs oracle/decomp | `7ae915bf` → `fb217d1c` → `d88977f6` → `3c337153` → `ad92dc60` | 2026-07-15 |
| walk — POSE-MATCH (median 1.2° per-bone geodesic vs live oracle) | CLOSED-parity | `parity_pose_diff.py` geometry check, 40 oracle vs 62 soh frames | `ae7f5f0f` | 2026-07-15 |
| run — POSE-MATCH (median 1.5° per-bone geodesic vs live oracle) | CLOSED-parity | `parity_pose_diff.py` geometry check, 40 oracle vs 64 soh frames | `ae7f5f0f` | 2026-07-15 |

> **Caveat carried from the checklist (NOT a reopen, but scoped honestly):** backwalk, swim_dive,
> climb_hang/climb_updown reach MATCH via a `Force*`-hook BYPASS of the real N64 entry gate, not a
> natively-driven decode. The SELECTION is at parity (closed); retiring the bypass is tracked as RE
> debt in `docs/re-frontier.md` (`player.backwalk-decode` = the one ⛔ hack) + `docs/re_control_debug_backlog.md`.
> Pose parity beyond walk/run is unproven (no live oracle for gated states) — those rows are
> SELECTION-closed, POSE-open (listed under OPEN below).

### MM (native 2S2H) — seed states (from `docs/mm_parity_checklist.md`)

| Item | Status | How verified | Verifying commit | Date |
|---|---|---|---|---|
| MM Link idle / walk / run via `Zelda3D_PlayerForce*` hooks | CLOSED-parity (by-construction) | Force* hook installs the literal RE'd body of the real installer (`func_80839E74`/`func_8083A794`) — verified by reading the installer, no external oracle exists yet | `f5740ff1` | 2026-07-15 |

> MM parity is "by construction" (matches the decomp source it was ported from), NOT oracle-verified
> against MM3D visuals — no MM3D player oracle exists yet. This is a weaker CLOSED than the OoT rows;
> a real MM3D A/B would upgrade it. It is closed only in the sense "the ported state = the decomp's
> state"; the whole MM visual-parity surface is OPEN.

### Gameplay scene parity (Kokiri Forest) — closed 2026-07-22

| Item | Status | How verified | Verifying commit | Date |
|---|---|---|---|---|
| OoT3D env palette reaches GAMEPLAY (was title-only) | CLOSED-parity | oracle A/B frame mean 62.7 vs 72.6 after, from 29.8 | `15284de5` → `ea3f39ab` | 2026-07-22 |
| ZSI env record layout (16B header + 28B records, amb @+0x0a) | CLOSED-parity | raw ZSI + oracle runtime list + live `LightAmbientColor` uniform, three ways | `ea3f39ab` / decomp `3b8e8f3` | 2026-07-22 |
| Outdoor blend source (`Zelda3dEnvBlend`, not `unk_BD`) | CLOSED-parity | time response restored: night (51,79,160), noon (181,181,160) | `ea3f39ab` | 2026-07-22 |
| 3DS PICA distance fog (fogNear/fogFar/zFar from ZSI) | CLOSED-parity | LUT solved from captured projection; node check byte-exact | `2389731c` | 2026-07-22 |
| Actor light-DIRECTION sign (3DS stores travel dirs) | CLOSED-parity | oracle actor uniforms; matched-camera A/B | `2389731c` | 2026-07-22 |
| 3DS camera values (all 66 settings; vertical FOV 52°) | CLOSED-parity | live Camera struct + captured projection; row alignment −17 → 0 | `381d1209` | 2026-07-22 |
| En_Elf fairy (wings + glow transform + proximity fade) | CLOSED-parity | draw-list structural identity vs oracle handles; matched-camera A/B | `cd1f4f46` / decomp `46b03ce` | 2026-07-22 |
| Kokiri Forest overall frame parity @0x6000 | CLOSED-parity — **per-draw verified** | per-draw masks vanilla-on-vanilla: every large surface within ~1.5% of the oracle; frame mean ours (89.3, 97.2, 26.1) vs oracle (89.3, 98.3, 29.7) | `5aedfe07` | 2026-07-23 |
| Kokiri BLUE channel (frame) | OPEN | frame mean blue 26.1 vs oracle 29.7 (−12%) with R exact and G within 1.1%; may share a cause with the Zora translucent-layer blue bias | — | 2026-07-23 |
| Kokiri NEAR-terrain brightness | CLOSED-parity | per-draw mask d8 **1.183 → 1.002** (126682 exclusive px), vanilla both sides; cause was per-fragment vs PICA's per-VERTEX vertex-colour saturation | `5aedfe07` | 2026-07-23 |

**Why the Kokiri row was reopened the same day it was closed:** it was closed on a FRAME MEAN, and a frame mean cannot see a per-surface error — a +19% overshoot on near terrain was being cancelled out in the average (and, separately, masked by a hi-res texture pack darkening our side). Per-draw pixel masks (`tools/tev_mask_ratio.py`) are the metric that exposed it. **Do not close a render row on a frame mean again.**

**Scope note:** these were verified AT KOKIRI FOREST. The changes are global (102-scene palette,
global camera table, global fog path); a multi-scene sweep is the outstanding check.

### Lighting / terrain — CLOSED **and OFF-LIMITS to tuning**

| Item | Status | How verified | Verifying commit | Date |
|---|---|---|---|---|
| World lighting engine port (#111) | CLOSED-parity — **DO NOT TUNE** | memory `soh3d-lighting-port` (#111 RESOLVED), direct user instruction to stop tuning | — (pre-map) | — |
| envCtx layout (play+0x3135, unk_BF +0xA5, stride 0x1C, Env_Update FUN_0045dd30) | CLOSED-parity | RE'd from decomp (not a memory poke) | — (pre-map) | — |
| Fog LUT (world) | CLOSED-parity | investigated as suspected gap → FALSE ALARM, already ported 2026-07-10 | `ac47d649` (note correction) | 2026-07-14 |
| Title terrain brightness (per-enabled-light ambient) | CLOSED-parity — **DO NOT TUNE** | see title arc `02181072`; terrain warmth residual is OFF-LIMITS | `02181072` | 2026-07-10 |

### Infrastructure / non-visual (closed capabilities)

| Item | Status | How verified | Verifying commit | Date |
|---|---|---|---|---|
| Texture pack (CMB by Citra CityHash64) | CLOSED | memory `soh3d-texpack` | — | — |
| PC-native input scheme + keyboard/gamepad HUD glyph hotswap | CLOSED | memory `soh3d-input-scheme`, `soh3d-hud-glyphs` | — | — |
| Keyboard game-input (in-game + title, ImGui ActiveId fix) | CLOSED | `65acc6c5` fixed dead keyboard input | `65acc6c5` | — |

---

## OPEN — NOT yet at parity (live work; safe for sweeps/loops to pick up)

| Item | Status | Where tracked | Notes |
|---|---|---|---|
| Title fire-glow flame-SHAPE residual + terrain-warmth | OPEN — but **OFF-LIMITS to tuning** | journals `bd9fc251`, `65373fa0` | The mechanism is ported (closed); the remaining shape/warmth residual is not to be micro-tuned per user instruction — reopen the mechanism only with a real RE finding, not a constant tweak. |
| Keyboard (headed mode) | OPEN — **blocked on user** | journal `2026-07-15-keyboard-headed-v2.md` | Headed keyboard path; blocked awaiting user. Do not loop on it. |
| Epona bone-level diff follow-up | OPEN | `d7e636c9` (boneinfo REPL + compare-titleactors tooling landed) | Tooling exists; the bone-diff itself is a follow-up investigation, not yet closed. |
| Link pose parity beyond walk/run (gated states: attack, roll, climb, etc.) | OPEN (SELECTION-closed, POSE-open) | `docs/link_parity_checklist.md` pose column (N/A rows) | No live pose oracle for gated states (equipment-less oracle save can't reach them). Needs a rendered-crop A/B or state-specific pose recipe. |
| Link state `ztarget` | OPEN (UNREACHABLE) | `docs/link_parity_checklist.md` | No En_Dekubaba (0x55) near Deku Tree spawn to drive it — reachability gap, not a divergence. |
| The `⛔ hack` + RE-debt items (backwalk native decode, camera-mode readout, putdown state, swim-dive gate, etc.) | OPEN | `docs/re-frontier.md` hacks list + `docs/re_control_debug_backlog.md` #1-#10 | SELECTION is closed for these Link states; driving them natively (retiring the Force* bypass) is open RE. |
| MM force-hook layer (Zelda3D_PlayerForce* state coverage) | PARTIAL — 21 states landed | `docs/re-frontier.md` `mm.force-hook-layer`; `debug_journal/2026-07-17-mm-force-hooks-*.md` | **Updated 2026-07-17:** 21 states ported + runtime-verified + regression-swept (`tools/mm_state_sweep.py`), far beyond the stale "idle/walk/run only". Remaining: transformation-specific (Goron/Zora/Deku) states + the mm_sweep parity oracle. The ~83 numbered player funcs are identified on-demand per behavior via OoT Rosetta, not a blocker. |
| MM3D skinned-actor rendering (SkelAnime intercept → 3DS CSAB anim → skinned draw) | OPEN — anim table LANDED (2026-07-21): `kMMAnimMaps` 4 → **1411** entries across 192 objects (1386 at first landing), 131 actors FULLY mapped; still gated `ZELDA3D_MM_SKINNED=1` | `2ship/2s2h/zelda3d/mm3d_animmap.inc` (generated by `tools/gen_mm_animmap.py`); `docs/MM_SKELANIME_PORT.md` | The table is no longer the blocker. It is generated from the 2ship asset XMLs' original-name annotations (four dialects; see below) — the decomp authors' record of each animation's ORIGINAL (romaji) name, which is exactly what the MM3D GAR calls the clip; that is authoritative rather than heuristic and is the only signal crossing the English↔Japanese naming gap. Every emitted clip is verified present in its actor's GAR (0 missing). Live Clock Town run: unmapped animations fell to ONE (an `overlays/` symbol, documented gap), 11 MM3D models animating, no crashes. **REMAINING BEFORE UN-GATING:** per-animation playback correctness is unverified (selection is right by construction, timing/phase is not spot-checked), and **CORRECTED 2026-08-12: the '~40 partially mapped' here was wrong** -- it counted Tex/UV/eye/mouth symbols (not skeletal animations) and objects that cannot take the skinned path at all (no MM3D GAR, or a GAR with no CSAB clips). Measured properly by the generator's un-gating coverage section: FULL 125 actors, PARTIAL 22, ZERO 9, excluded 36. **UPDATED 2026-08-12 after a second generator pass: 1411 entries, FULL 132 / PARTIAL 15 / ZERO 9 / excluded 36.** Achieved as the plan of record required -- by fixing the GENERATOR's matching rules, never by hand-editing the .inc: the annotation scanner now reads all trailing comments rather than only the first, understands three further annotation dialects (an explicit MM3D-side name, a NEGATIVE "not present in MM3D" that suppresses guessing, and the corpus's two misspellings of "Original"), tolerates a mis-placed closing quote, and carries eight measured-and-cited `gameplay_keep` door mappings in a self-checking VERIFIED_OVERRIDES table (`--verify-overrides`, claim C083). Net +26 mappings and -1 FABRICATED one: `gOdolwaJumpDanceAnim`, annotated "Not present in MM3D", had been stealing `dance_jump` from the symbol actually annotated with it. The generator now reports per-rule firing counts with a denominator so a rule that stops firing is visible. The remaining 16 PARTIAL are mostly one symbol each and are precisely the residue of rules an adversarial pass REJECTED with firing counterexamples (frame-count bipartite matching, form-prefix lexicon, `<CurveAnimation>`, limb-arity, offset ordering — see re-frontier `mm.skinned-csab`); leaving them unmapped is the correct outcome, not a gap to close by loosening a rule. PARTIAL remains the real risk (animating for some actions, idling for others reads worse than uniform N64); next step is to make whatever is still partial all-or-nothing per actor. |
| Camera modes beyond Normal1 (Normal0, Parallel*, …) | OPEN | `docs/re-frontier.md` `camera.normal0-and-others` | Only CAM_FUNC_NORM1 ported; all others fall through to legacy N64 math. |
| Full-scene visual parity (non-title scenes: water, skybox-through-wall edge cases, etc.) | OPEN | GitHub Issues kanban (`tools/kanban.py ls`) | User-driven scene parity cards live on the kanban, not this map. |
| Object→ZAR coverage: 67 of 377 objects unmapped (cutscene rigs, effects, scene bundles) | OPEN | `COVERAGE.md` | See COVERAGE.md category breakdown for which need which path. |

---

## Cross-links (the four-doc orientation system)

- **`docs/codemap.md`** — what CODE exists + coarse subsystem status (find where a thing lives).
- **`docs/re-frontier.md`** — ordered RE progress: re-verified vs ⛔ hack vs todo, next RE step.
- **`docs/parity-map.md`** (this) — CLOSED-CASES registry: what's proven at parity, don't revisit.
- **`docs/parity-workflow.md`** — the METHOD for moving an item from OPEN → CLOSED (oracle A/B at
  content-matched frames).
- Generated detail feeding this map: **`docs/link_parity_checklist.md`** (auto, `link_sweep.py`) +
  **`docs/mm_parity_checklist.md`** (hand seed). User-driven work items: the **kanban** (GitHub
  Issues / `tools/kanban.py`, `KANBAN.md`).

## Follow-up (noted, not done this pass)

A generator that aggregates the MATCH rows from `link_parity_checklist.md` (and `mm_parity_checklist.md`
once `mm_sweep.py` exists) into this map's CLOSED table format would keep the Link/MM sections from
drifting from the live sweep. Worth building once the sweep set stabilizes; until then this map is
hand-authored and the checklists are cited as the live detail. The title/scene/lighting rows are
inherently hand-authored (they come from journal + commit history, not a sweep) and would stay so.
