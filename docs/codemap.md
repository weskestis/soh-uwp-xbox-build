# SoH3D codemap — what's where, what's done, what's missing

For the project's overall shape + naming (the **zelda / zelda3d × soh / 2ship** taxonomy and where
each part lives), see `docs/project-structure.md`. This file is the per-subsystem detail beneath it.

The single-page orientation map. Consult this FIRST at the start of any task (find the
subsystem, its honest status, its entry point); update the relevant row in the SAME commit
that lands or changes a subsystem. Maintained with `tools/codemap.py` (`tree` regenerates the
annotated source tree below, `check` flags drift between this table and the real tree — run it
before committing a structural change).

Companion docs (read together, cross-linked, see bottom): **`docs/parity-map.md`** (the
CLOSED-CASES registry — what's confirmed at parity and must NOT be re-examined unless the user
reopens it), **`docs/re-frontier.md`** (ordered RE progress: which behavior is real
reverse-engineering vs a hack standing in for it),
**`docs/parity-workflow.md`** (the oracle-driven method used to CLOSE a gap this map records),
**`KANBAN.md`** (local board — user-driven work items, NOT a dumping ground for sweep findings,
see `CLAUDE.md`), **`docs/re_control_debug_backlog.md`** (N64-decomp control/debug gaps that
block sweep coverage — downstream of RE, not ground truth itself).

## The big picture

SoH3D renders **OoT3D (3DS) models, world, and behavior** through **Shipwright/SoH's N64
decomp engine** (`Shipwright/soh/src`) as the runtime spine — game logic, actor update, camera,
collision all still run the N64 code faithfully; the **`zelda3d/` layer**
(`Shipwright/soh/src/zelda3d/`) intercepts at draw time and at specific behavior seams to
substitute the 3DS asset/animation/camera-math/lighting, falling through to legacy N64
rendering wherever a behavior hasn't been ported yet. Ground truth for any divergence is the
**OoT3D decomp** (`oot3d-decomp/`, a private submodule fed by Ghidra RE), not memory-probing SoH
at runtime — see `docs/re-frontier.md` for what's actually been RE'd vs assumed.

A parallel, much younger arm ports **Majora's Mask 3D** the same way, but on a *different*
runtime spine: MM uses the native **2S2H decomp** (`2ship/`, `2s2h/`) rather than a
patched N64-ROM engine, sharing only `libultraship`/the renderer with the OoT side. The two
share vision and some tooling patterns but are separate ports at different maturity.

**Core gap right now:** the OoT3D render/behavior layer covers most named actors and the title
sequence in depth, but full-scene visual parity (lighting, water, skinned-actor edge cases) and
the MM player port are still open; see the per-subsystem table below for what's actually
verified vs partial vs stub.

## Build layering (configure root = the REPO ROOT)

`cmake -S . -B Shipwright/build-cmake -G Ninja`. The root `CMakeLists.txt` is the project root and
the two games are **peers** under it; see `docs/project-structure.md` for the full picture and for
the two mechanisms available for sharing code between them.

```
launcher   Shipwright/zelda3d_app     zelda3d — dlopens a game core (RTLD_LOCAL), holds no game code
games      Shipwright/soh (OoT)  ·  2ship (MM)          ← peers, neither hosts the other
shared     Shipwright/zelda3d_shared  static lib (no game types) + gui/ init/ (headers)  +  port/ extractor/ gui/ (shared source, built per game)
engine     Shipwright/libultraship (libultraship.so, ONE copy)  ·  Shipwright/cmb3d
```

Before 2026-08-06 the configure root was OoT's own directory, which made MM a guest of OoT's build
and let `2ship/CMakeLists.txt` reach the engine's assets through `${CMAKE_SOURCE_DIR}`. Paths are
now named variables set by the root (`ZELDA3D_ENGINE_DIR`,
`ZELDA3D_OOT_DIR`, `ZELDA3D_MM_DIR`, `ZELDA3D_ENGINE_ASSETS_DIR`, `ZELDA3D_SHARED_DIR`), each with a
standalone-configure fallback in the game that uses it. ccache is wired in as the compiler launcher
when present (`-DZELDA3D_CCACHE=OFF` to opt out).

CMake keeps developer ROM extraction/header generation in `cmake/Zelda3DAssetExtraction.cmake`
separate from the always-current shipping archives in `cmake/Zelda3DRuntimeArchives.cmake`.
`zelda3d_app` regenerates `soh.o2r`; `launcher_bootstrap/mm_assets.py` owns MM's atomic developer
`mm.o2r`/`2ship.o2r` extraction and its ROM-free release `2ship.o2r` generation, and
`launcher_build.py` validates that all runtime artifacts exist. `tools/build_mm_custom_archive.py`
is only the container-callable entry point to that shared MM owner.
Shared CMake cache/Clang/Ninja/configure policy belongs only to
`tools/cmake_build_policy.py`, which both launcher and harness build entry points call.

Release ROM provisioning belongs to `Shipwright/zelda3d_shared/platform`: one content identity
authority validates direct files and nested ZIP entries, `RomSelectionStore` commits accepted
selections under the OS configuration directory, and the SDL setup owner presents Browse/Quit.
Both N64 extractors consume the activated launcher environment; neither scans the working tree for
an alternate ROM. `tools/build_appimage_release.py` owns the reproducible Ubuntu 22.04 release
build; `tools/package_appimage.py` plus `packaging/` own staging and artifact verification, with
content and ABI gates that refuse ROMs, ROM-derived archives, and binaries newer than the declared
glibc/libstdc++ baseline.

## Source tree (top level, `tools/codemap.py tree`)

```
Shipwright/soh/src/          465,661 lines, 1719 files
├─ zelda3d/                   38,030 lines  449 files   [.h .cpp .c]     # OoT3D render/behavior layer (THIS is the SoH3D-specific code)
│  ├─ core/                         844 lines    9 files              # lifecycle/math/runtime composition
│  ├─ model/                      2,389 lines    6 files              # CMB/CSAB load + skinning + overlay2d
│  ├─ anim/                       2,120 lines   19 files              # playback/retarget plus focused pose evaluation/tracking
│  ├─ player/                     1,995 lines   22 files              # composed Link draw/pose/control/retarget/REPL owners
│  ├─ cutscene/                    1,117 lines    3 files              # title-cs interpreter + opcode reference
│  ├─ behaviors/                  8,359 lines  110 files              # actor/camera/title owners; actor subtree includes focused BossFd modules
│  ├─ repl/                       5,261 lines  144 files              # 105-line composition router plus focused runtime/command owners
│  ├─ remaining shown dirs       15,918 lines  135 files              # assets/control/diagnostics/hud/input/launcher/lighting/render/scene
│  └─ zelda3d.h                      27 lines    1 file               # compatibility-only umbrella; new code includes focused owner headers
├─ overlays/                 309,355 lines  977 files   [.c .h]       # vendored N64 OoT decomp (actors/effects/gamestates/misc)
├─ code/                      111,501 lines  152 files   [.c]         # vendored N64 OoT engine core, patched only at narrow zelda3d seams
├─ libultra/                    5,513 lines  125 files   [.c]         # vendored N64 libultra shim
├─ boot/, buffers/, dmadata/, elf_message/    small, vendored N64 boot glue

Shipwright/libultraship/     144,544 lines  725 files
├─ src/ship/  + include/ship/  ~30,942 lines  249 files                # Ship:: generic framework + focused Zelda3D UI bridges
├─ src/libultraship/ + include/libultraship/   ~9,727 lines  70 files  # LUS:: concrete N64 impl (controller mapping, N64 button semantics)
├─ src/fast/ + include/fast/                  ~21,602 lines  72 files  # Fast3D + focused Zelda3D renderer contracts/backends
└─ extern/StormLib/                            76,617 lines  308 files # vendored MPQ archive lib (unrelated third-party dep)

2ship/                (2S2H-based MM native port; separate runtime spine from OoT)
├─ 2s2h/                       MM-specific glue (ShipInit, Z3DBoot, composed zelda3d/repl/, other zelda3d owners)
├─ src/, include/              vendored 2S2H N64-MM decomp (z64player.h, z_player.c, ...)
└─ assets/                     MM asset extraction/build

oot3d-decomp/                 (private submodule) 69 docs under docs/ — the OoT3D ground-truth RE corpus
mm3d-decomp/                  (private submodule) 3 docs under docs/ — MM3D ground-truth RE corpus (much younger)

launcher_bootstrap/          Python owners for launcher arguments, layout, asset provisioning, native-dependency refusal, and runtime environment

tools/                        39,249 lines  367 files   # sweep/parity/harness/build-policy tooling (see Tooling row)
├─ tools/clang_verifier/         1,081 lines   14 files  # structure, compile-db, format, and clang-tidy owners + tests
├─ tools/mm_animmap_tests/       MM animation-map archive/inventory/matching/output/verification contract tests
└─ tools/soh3d_harness/          9,459 lines  151 files  # embedded oracle composed from focused C++ modules

debug_journal/                 dated findings entries — the append-only investigation log (NOT this codemap's job to duplicate)
```

## Subsystem status

| Subsystem | Status | Where | Deep doc | Gap / next |
|---|---|---|---|---|
| **Renderer — unified CMB material route** | 🟡 active; generic TEV complete | CMB coordinator, attribute, material, and sampler parsing/resolution in `Shipwright/cmb3d/asset/cmb.{h,cpp}`; shared sampler policy and vertex layouts in `Shipwright/libultraship/include/fast/{zelda3d_sampler,zelda3d_model_types,unified_vtx}.h`; native/unified pipelines, UBOs, and shaders in `Shipwright/libultraship/{include,src}/fast/` | `debug_journal/2026-08-14-en-vb-ball-and-unified-tev.md`; `debug_journal/2026-08-28-cmb-secondary-uv-sources.md`; `debug_journal/2026-08-30-{cmb-authored-sampler-filters,cmb-unlit-primary,cmb-lit-primary-alpha,cmb-fragment-lighting-disabled}.md`; `oot3d-decomp/docs/{pica_tev_combiner,title_env_lighting,fragment_lighting,boss_fd2}.md` | `kGenericTev` carries the verified legacy packer's exact six-stage PICA words, RGBA8 constant palette after per-actor overrides, three texture units, coordinator transforms, and final-alpha compare. Each PICA coordinator's byte-0 `sourceCoordinate` preserves its independent CMB `texCoord0/1/2` stream. Each texture unit preserves the binding's binary-authored min/mag filter; one neutral resolver derives nearest/linear and none/nearest/linear mip selection, and the backend cache keys the complete sampler state. Native and unified CMB PRIMARY preserve both exact `HasColor` branches: unlit no-color draws retain authored MatDiffuse RGBA, while lit draws sum diffuse alpha per enabled light before optional color multiplication. Vertex PRIMARY and fixed-function fragment primary/secondary now remain distinct through shared TEV evaluation; the exact disabled zero/zero branch covers five retail consumers, while the enabled 197-primary/69-secondary path remains RE-partial on its PICA config/LUT state. Dungeon-candle, bottled-Poe, and Dark Link close-tests lock these parser-to-shader contracts. Wider unified-renderer feature gaps remain tracked by the renderer campaign. |
| **zelda3d runtime composition / model-animation-scene layer** | 🟡 partial; ✅ transactional graphics selector and replacement fallback contract | `core/zelda3d.c` (composition), `core/zelda3d_runtime.{c,h}` (persistent Original/OoT3D request + same-entrance transition/`Play_Init` commit), focused `control/`, `diagnostics/`, `lighting/`, `scene/`, `render/`, `player/`, and `anim/` owners; CCI/extracted-RomFS source interface in `Shipwright/cmb3d/asset/ctr_rom.{h,cpp}`; source resolution/status and model/texture preflight in `model/zelda3d_model.cpp`; authored-CSAB preflight in `anim/authored_playback.h` + `anim/zelda3d_anim.cpp` | `docs/cursor-fps-v3-oot3d-full-build.md`; `debug_journal/2026-09-04-cursor-fps-v3-oot3d-full-build.md` | The graphics choice is persisted and committed only after the old PlayState/collision is destroyed. A private owner-extracted `oot3d-romfs` directory or decrypted `oot3d.3ds` is identity-checked and reported in Settings; encrypted CCIs are rejected. Replacement emitters prove model, texture, and required animation readiness before suppressing the N64 draw; incomplete parts fall back per object, while multipart sky/celestial/Volvagia composites fall back as a unit. Live Original→OoT3D→Original scene reloads passed against the extracted source. The former core/player/render grab-bags are retired. Object→ZAR coverage remains 310/377 mapped objects (`COVERAGE.md`); 67 objects have no OoT3D model path and intentionally remain N64 fallback. |
| **CURSOR FPS V3 controller cursor + complete SoH menu** | 🟡 merged; ✅ optimized build, contract gate, and live menu arbitration pass; physical Xbox input pending | `Shipwright/libultraship/include/fast/backends/cursor_fps_v3.h`, `Shipwright/libultraship/src/fast/backends/cursor_fps_v3.cpp`, SDL lifecycle/event seam in `Shipwright/libultraship/src/fast/backends/gfx_sdl3.cpp`, input-poll wrappers under `Shipwright/libultraship/src/{ship,libultraship}/controller/`, menu arbitration in `Shipwright/libultraship/src/fast/Fast3dGui.cpp` + `Shipwright/libultraship/src/ship/window/gui/rml/SohRmlUi.cpp`, full menu builders under `Shipwright/soh/soh/SohGui/SohMenu*.cpp` | `docs/cursor-fps-v3-oot3d-full-build.md`; `debug_journal/2026-09-04-cursor-fps-v3-oot3d-full-build.md`; gate `tools/test_cursor_fps_v3_full_build.py` | Reproduces the recovered V3 350 ms L3+R3 toggle, input suppression, right-stick cursor/A click, feedback, and overlay constants. View/F1 opens the complete SoH menu; Start/Escape owns the compact RmlUi menu; ownership and capture are mutually exclusive. Both menu directions and the full settings draw passed live on SDL3 GPU. SDL3 renderer no-ops and no-I/O networking are visibly unavailable rather than silently accepted. A genuine Xbox Device Portal AppX remains blocked on a separate SDL2/UWP renderer target plus Windows SDK signing and device validation; repacking the signed V3 payload is invalid. |
| **Actor-behavior framework (OOP registry)** | ✅ framework done; 🟡 coverage partial; ✅ BossFd forced-profile producer/render live | `behaviors/actor_behavior.{h,cpp}` (registry/dispatch), `actor_behavior_bridge.h` (C seam), `behaviors/actor/{npc_draw,rupee_draw}.*`, `behaviors/actor/boss_fd.{h,cpp}` (render orchestration), focused `behaviors/actor/boss_fd/authored_flight.{h,cpp}` plus sibling history/control/effect owners, and `anim/pose_evaluation.*` | `CLAUDE.md` game-structure rule; `oot3d-decomp/docs/boss_fd2.md`; `debug_journal/2026-08-27-renderer-template-and-mm-player-animation.md` | Registry, C dispatch, NPC pose, and rupee drawing have separate owners. BossFd produces authored movement/history at 30 Hz from the pre-update seam for every instance with per-actor `ObjectExtension` state; comparator validation remains scoped to the constant forced profile. The linked Clang shipping/harness run reports exact producer/history `MATCH`, and the paired held-camera images render the full flying body in both engines. General action/death production and quantitative image parity remain open. |
| **Camera-behavior framework** | 🟡 values ported wholesale, one mode verified; at-default Y-bias in progress | `behaviors/camera_behavior.h/.cpp`, `behaviors/camera/normal1.cpp`, `behaviors/camera/at_default.{h,cpp}`, `behaviors/camera/at_default_policy.{h,cpp}`, SoH-owned `Shipwright/soh/tests/camera_at_default_policy_tests.cpp`, `tables/zelda3d_camera_values.inc` (generated by `tools/gen_oot3d_camera_values.py`), narrow seams in `code/z_camera.c` and the Player overlay | `oot3d-decomp/docs/camera_calc_at_default.md`, `camera_math_helpers.md`; `debug_journal/2026-08-24-camera-at-default-ybias.md` | The full 66-setting 3DS table and `CAM_FUNC_NORM1` remain the verified base. The focused `at_default` module now ports Grezzo's static-floor upward-rise latch, exact 30:20 decay cadence, and camera-at consumer using typed fields and per-player state. The pure policy seam locks the recovered get-item exception to slope-floor ownership without teaching the generic engine about game behavior. It is not yet built or live A/B-verified; threshold timing at the host's 20 Hz observation boundary remains the honest gap. Other camera function bodies still fall through to N64 math. |
| **Title presentation (cs sequence)** | 🟡 partial; actively tightening | `behaviors/title/title_presentation.{h,cpp}` (51-line composition) plus focused `title_{activity,camera,rider,rider_state,atmosphere,lighting,overlay,logo,cloud_vortex,fireglow}.*` owners; cache-first evidence in `tools/title_{host_capture,oracle_probe,oracle_context}.py` | `oot3d-decomp/docs/title_*.md` (26 docs — the largest single RE arc), `debug_journal/2026-07-1[4-5]-title-*.md`, `debug_journal/2026-08-30-title-unified-cmb-state-contract.md` | Scene = spot99 (not spot00), camera/rider and coordinator-1 decoration behavior remain verified. Exact identities falsified both the historical mat10/11 self-add override and the inferred live-camera sphere basis: those groups use one sampler, authored TEV, coordinator-0 sphere mapping, and CmbVShader c4-c6 identity. The selected host TEX0 sample now matches the binary-authored linear-filter result; wider title composition remains visibly divergent. The presenter composes domain owners and contains no activity/camera/rider/atmosphere/lighting/overlay implementation. |
| **En_Horse / Epona (title + world)** | ✅ module landed (2026-07-16); 🟡 render-gap remainder | `behaviors/actor/en_horse.{cpp,h}` — `Zelda3D_HoofDustWorldPos` (hoof-dust Y reconcile) + `Zelda3D_HorseSaddleOffset`/`Zelda3D_EnHorse_RecordDraw` (#152 rider seat); title motion in `behaviors/title/title_rider.*` | `oot3d-decomp/docs/en_horse_epona_render_gap.md`, `en_horse_hoof_dust.md`, `en_horse_rider_pos.md`, `en_horse_title_gallop_rate.md`, `debug_journal/2026-07-15-epona-*.md` | The two draw-adjacent behaviors are consolidated into the per-actor module (satisfies the CLAUDE.md structure rule); verified live spawning Epona in Kokiri Forest (relocated saddle offset fires, model renders). Remaining open item is the Skin_DrawImpl body-render hook (Epona's body still draws as native N64 mesh) — the render-gap remainder, `enhorse.render-gap` on the frontier. |
| **Player / Link (draw + pose + 3DS behavior)** | 🟡 draw done; draw-ANCHOR mechanism RE'd+ported 2026-07-23 (age root-translation scale 0.64 + shape.yOffset term; per-frame feet-grounding DELETED — it was the walk-vibration and climb-warp bugs; door-exit slide fixed via unk_868 leg-driver selection; see re-frontier `player.draw-anchor`); 14 OoT3D divergences ported (2026-07-22), 8 blocked on engine-side RE | `player/zelda3d_link.cpp` (composition), `player/{player_draw,player_draw_policy,player_midmask,player_retarget,player_pose_scan,player_control,player_repl}.*`, `player/zelda3d_link_face.*`, `player/zelda3d_sword_trail.*`, `player/player_behavior.h`, generated player tables, and narrow vendored decomp seams | `debug_journal/2026-07-22-oot3d-player-port-campaign.md`, `oot3d-decomp/docs/{player_port,divergence_map}.md`, `docs/link_parity_checklist.md` | **A wholesale z_player.cpp port is the WRONG shape** — 730 OoT3D functions swept and every Link bug chased to ground was byte-exact to N64; Grezzo did not rewrite Link. The real payload is the catalogued DIVERGENCES. A 65-agent sweep turned 41 of them into specs: **14 ready (ALL PORTED)**, 10 rejected by adversarial verification (42% of implementable ones), 10 not divergences at all, 8 blocked. Ported: buoyancy 2/3 rescale, Iron-Boots vetoes (talk/A-press), Zora-tunic boot regs, first-person entry pose, two camera SFX deletions, sword-trail tip trim, jump sword-clank, Lens obtainability, plane epsilon, dead-input gate, guarded Master Sword regive, in-water item buttons. **`divergence_map.md` is a HYPOTHESIS, not a work list** — 5 rows named the wrong function outright and were corrected. Verification: smoke test passes; all 25 sweep states MATCH — but that sweep is **anim-SELECTION only** (CSAB substring match, pose_verdict N/A), so it proves no selection regressions and cannot see physics. **Buoyancy port CONFIRMED BY MEASUREMENT** (2026-07-22): 90-frame `asample` in Zora's Domain water reads a per-frame velY increment of 0.0666-0.0667 (ported 0.06666667f; N64 is 0.1f), and the damping term matches `(-velY*0.2)+0.0667` to four decimals at every sampled point. That confirms the constants are LIVE and correctly signed; it is not yet an oracle A/B. Recipe: `warp 0x109 ; tp -1094 -160 -340 ; asel link ; asample 90 <csv>` (Lake Hylia is DRAINED in this save — use Zora's Domain). Remaining 13 ports unmeasured; several are gated on equipment the REPL cannot grant. |
| **Shared Link policy (game-agnostic)** | ✅ scaffold + adult mesh-mask migrated; equipment-visibility gaps closed 2026-07-29; dr_libs hoisted 2026-08-05 | `Shipwright/zelda3d_shared/` — `Shipwright/zelda3d_shared/audio/dr_libs_impl.cpp` (the ONE copy of dr_wav/dr_mp3/dr_flac for the whole project; both games previously baked their own 158-function copy into their game core via `DR_*_IMPLEMENTATION` in their AudioSampleFactory.cpp, and the root CMakeLists now declares the `dr_libs` FetchContent once instead of soh and 2ship each declaring it), `player/link_gear.h` (LinkGear POD: game-agnostic per-frame equipment/hand-pose snapshot), `player/link_midmask.{cpp,h}` (adult mesh-mask + retarget policy); static lib `zelda3d_shared` | memory `soh3d-mm-oot-link-unify` | Cross-branch library shared by OoT (`soh`) and MM (`2s2h`): nothing here sees a game-specific type (`Player`/`Actor`/`PlayState`) — each game's adapter converts native structs to the flattened enums/POD. Stage 2a landed the scaffold + adult mesh-mask; 2b/c grow it. MM forms stay MM-only. **2026-07-29 equipment sweep**: three sets of meshes existed in the CMBs and were NEVER ENABLED — adult gauntlet plates (4, 17, 5|6, 18|19, gated `strengthUpgrade >= 2`), the child Goron bracelet (15, gated `>= 1` — the bracelet IS upgrade 1), and iron/hover boots (35,36 / 15,22). `LinkGear` gained `strengthUpgrade` and `boots` for them. Gold gauntlets additionally needed a TINT: OoT3D writes a per-upgrade colour from a two-row table (silver `(1,1,1,1)` = identity, gold `(0.996,0.812,0.059)`), applied here via `Zelda3D_GL_SetMatConstOverride` on adult material 14, `constIdx=5`. Silver being the identity is why the missing colour path was invisible for years. All four verified live with frozen-logic pixel measurement; see `debug_journal/2026-07-29-adult-gauntlet-plates-never-drawn.md` and the RE in `oot3d-decomp/docs/player_draw_impl_located.md`. KNOWN REMAINING: the child bracelet renders dark (narrowed to its TEV chain, not the material or texture), and the full reset-then-enable architecture OoT3D actually uses (claim C010) is still unported — our hand-curated mesh map remains the mechanism, now with these gaps filled. |
| **Skinned-actor rendering (CMB rigid skinning)** | ✅ verified | material/skin handling in `model/zelda3d_model.cpp` + CMB layer | `docs/skeletal_parity_backlog.md`, `docs/material_facial_channel_spec.md` | #109 (skin_mode 1 per-vertex single-bone) and #107 (skinned-actor collision at DrawSkeletonOpa) both closed per memory. Not re-audited this pass — trust memory, spot-check if a new divergence surfaces. |
| **Scene lighting / fog / world environment** | ✅ at parity (Kokiri verified) | generated `tables/zelda3d_scene_lighting.inc`, override ownership in `lighting/zelda3d_lighting.{c,h}` and focused render lighting/fog owners, `Zelda3dEnvBlend` capture in `code/z_kankyo.c` | `docs/oot3d_world_lighting_re.md`, `oot3d-decomp/docs/env_context_layout.md`, `oot3d-decomp/docs/ram_map.md`, memory `soh3d-lighting-port` | 2026-07-22: the ported palette had NEVER reached gameplay (z_kankyo had only a title hook), and the ZSI records were misparsed (16-byte header read as a 28-byte "metadata entry 0"; the old "+1 slot bias" masked a 12-byte phase shift, so "ambient" was direction bytes). Both fixed; 3DS PICA distance fog ported from the same records; actor light-DIRECTION sign corrected (3DS stores light-travel dirs). Kokiri Forest frame mean 72.4 vs oracle 72.6. `gZelda3dFogEnable` (the F3DEX ramp disabled by #113) stays 0 — do not resurrect. Coefficient tuning remains banned. 2026-07-23 (unified shading): the OoT3D colour blend now goes straight to the CMB renderer and is NO LONGER written into `envCtx.lightSettings`; the N64 rows flow lightSettings→lightCtx again, so N64-format draws use their own calibration space. Dirs + fog stay shared via envCtx. One env state, two calibration palettes; both remain visible via REPL `lightparams`. |
| **Cutscene format / opcode interpreter** | ✅ format decoded, 🟡 port coverage partial | `cutscene/zelda3d_cutscene.cpp/.h`, `cutscene/zelda3d_cutscene_oot3d_opcodes.h` | `oot3d-decomp/docs/cutscene_format.md`, `title_writer_chains.md`, `title_basis_writer_jit_solved.md` | Opcode format and the title basis-writer JIT mechanism solved (a *dead-end* static-decomp path documented separately in `title_basis_writer_static_deadend.md` — don't re-chase static extraction). Coverage beyond the title cs not audited here. |
| **Renderer — SDL3 GPU (primary)** | ✅ primary backend; 🟡 unified route parity partial | `Shipwright/libultraship/src/fast/backends/gfx_sdl3gpu.cpp` plus focused startup owner `Shipwright/libultraship/src/fast/backends/gfx_sdl3gpu_initialization.cpp`; focused Zelda3D owners `zelda3d_sdl3gpu.cpp` (C ABI), `_resources.cpp`, `_pipelines.cpp`, `_pass.cpp`, `_lifecycle.cpp`, and `_shaders.cpp`; shared unified shader/UBO contracts under `include/fast/` and `src/fast/backends/unified_shader.cpp` | `SDL3GPU_MIGRATION.md`; `debug_journal/2026-08-27-renderer-template-and-mm-player-animation.md`; `debug_journal/2026-08-30-title-unified-cmb-state-contract.md` | GPU handle ownership remains checked through `NoteGpuRelease`. Fixed shaders compile during backend startup, which refuses failure rather than silently dropping native draws. The optional CMB-unified route preserves the CmbVShader sphere-normal matrix independently from host placement, the private RE'd light, three authored dual-texture modes, per-draw RGBA/alpha-test ordering, and independent coordinator-0/1 mapping. The removed synthetic fourth mode had conflated sphere-mapped TEX0 with an invented TEX1 alias. Cached exact-cursor evidence closes the mat10/11 normal/TEV/sample contract but does not certify the wider unified route. Model/texture completion emits an optional host heartbeat used by the harness watchdog; shipping hosts leave it unset. The generic TEV evaluator, auto-scale, footprint, and OPA/XLU routing remain as previously verified. |
| **Renderer — SDL2/OpenGL (Xbox/UWP profile)** | 🟡 real backend complete on Linux; signed AppX installed; first Xbox boot exits before render | `Shipwright/libultraship/src/fast/zelda3d_opengl_{resources,pass,state,hud}.cpp`, thin C ABI in `zelda3d_opengl.cpp`, dispatch in `zelda3d_{submission,model_provider}.cpp`, teardown in `backends/gfx_opengl.cpp`, public owner contract `include/fast/backends/zelda3d_opengl.h`, UWP build boundary and durable boot logger under `uwp/`, portable timing owners `Shipwright/soh/soh/host/frame_timing.cpp` and `Shipwright/soh/src/zelda3d/repl/repl_fps.cpp`, guarded FIFO transport `Shipwright/soh/src/zelda3d/repl/repl_transport.cpp`, narrowed HUD C ABI `Shipwright/soh/src/zelda3d/hud/zelda3d_hud_tex.cpp`, UWP object aggregation in `Shipwright/soh/CMakeLists.txt`, runtime/icon staging audit `tools/uwp_runtime_stage.py`, remote workflow `.github/workflows/build-xbox-uwp.yml`, live test `tools/zelda3d_opengl_smoke.cpp` | `debug_journal/2026-09-05-opengl-oot3d-renderer.md`; `debug_journal/2026-09-05-uwp-phone-build-pipeline.md`; `debug_journal/2026-09-06-xbox-first-boot-diagnostics.md`; `docs/cursor-fps-v3-oot3d-full-build.md` | Synchronous native draws preserve interpreter emission order. The runtime GLSL 1.40 program is derived from the authoritative fixed CMB shader and carries 64 bones, UV0/1/2, three samplers, generic six-stage TEV, material/UV overrides, lighting, sphere mapping, fog, per-group depth/blend/cull, and overlay-depth reset. An isolated VAO plus a complete save/restore boundary protects Fast3D state. The native HUD owns ordered modulate/ENV quad runs and its own texture cache. Mesa software-GL renders model and HUD pixels in a real 4.5 compatibility context; SDL2 and SDL3 full-core links pass. The package builds the large renderer/game core as normal x64 Windows with `ZELDA3D_UWP_CORE`, supported static vcpkg libraries, portable CityHash and monotonic `std::chrono` timing, GLEW, SDL audio, no legacy StormLib/WinINet, and packaged-DLL OpenGL loaders; the development-only POSIX FIFO is a no-op on Windows. Only the native `SDL_WinRTRunApp` wrapper/AppX graph is WindowsStore. It requires `.o2r`, imports pinned SDL2/Mesa binaries, disables ROM extraction, resolves private assets from `LocalState\soh`, signs with an ephemeral key, and exposes only the public certificate. Remote run 14 completed the entire 75-minute `soh_core.dll` compile/link, proving the object-library and nine-symbol corrections. Its later staging failure came from searching the CMake binary tree instead of Shipwright's configured `<source>/x64/Release` output. The three-job workflow preserves that ROM-free core separately and keeps package retries independent of the costly compile. Run 21 built, signed, installed, and accepted the private files, but the first physical Xbox launch exited immediately and Device Portal produced no dump. The wrapper now starts its durable `LocalState\soh\uwp-boot.log` before the window handoff, dynamically loads the core, logs the ABI boundary, and catches Windows exceptions; Xbox evidence from that report remains required before a usable-package claim. |
| **Renderer — Vulkan (legacy, being retired)** | 🟡 M2 done, superseded | referenced via `SOH3D_VULKAN=1`; per-combiner work in `src/fast/` | memory `soh3d-vulkan-port` | M2 per-combiner (Kokiri parity) done; M3 framebuffers was next but SDL3-GPU is now the mandated single path — confirm before investing further here. |
| **Renderer — GL / Fast3D core** | ✅ done (legacy, still active fallback) | `src/fast/` (~15,944 lines) | `soh3d-gl-widescreen-and-camtool`, `soh3d-gl-state-leak` memories | Widescreen X-squeeze mirrored from Fast3D `AdjXForAspectRatio`; known state-leak class fixed by resetting touched GL state per draw. |
| **libultraship — `Ship::` generic framework** | ✅ done, actively maintained | `include/ship/`, `src/ship/` (~29,700 lines, 237 files) | `docs/lus_input_architecture.md` | Base layer for input/window/resource; NOT duplicate of `libultraship/` — base/derived split by design (read the arch doc before re-deriving). |
| **libultraship — `LUS::` N64 concrete impl** | ✅ done | `include/libultraship/`, `src/libultraship/` (~12,100 lines, 73 files) | `docs/lus_input_architecture.md` | Controller/button-mapping classes, chord/modifier design (#32) documented in the arch doc. |
| **Launcher + game-core loading (ONE binary, both games)** | ✅ done — loading model verified 2026-08-05, in-process game SWITCH verified 2026-08-07 | `Shipwright/zelda3d_app/` (launcher exe `zelda3d`, holds no game code), `Shipwright/libultraship/include/ship/zelda3d_core.h` (the `Zelda3DCore` ABI), `Shipwright/soh/src/code/core_entry.c`, `2ship/src/code/main.c` (`Zelda3D_CoreRun`), CMake targets `soh_core`/`mm_core`/`zelda3d_app`, `Context::{SetAppBundlePath,RequestGameSwitch,TakeRequestedGameSwitch}`, `soh/src/zelda3d/launcher/` | `docs/MM_NATIVE.md` N3, claims C050/C054/C056 | Each game builds as a shared object exporting ONE symbol (`Zelda3D_CoreEntry`); the launcher dlopens it `RTLD_NOW \| RTLD_LOCAL`, which is what lets OoT and MM each define `Play_Init` (6,616 colliding decomp symbols — renaming them was never viable). `zelda3d --probe-cores` loads BOTH cores into one process, reports `loaded 2/2`, and checks known-colliding symbols resolve to DIFFERENT addresses per core; it exits non-zero on a partial load or on zero comparisons, and was validated in both directions. Both games verified reaching gameplay through it (`scratch/screenshots/launcher_{oot,mm}.png`). Note `Context::GetAppBundlePath` was exe-relative and therefore wrong under a launcher — the launcher now supplies the core's own directory from `dladdr()`; archives were unaffected (CWD-based). **2026-08-07 — the build now produces exactly ONE program.** `soh.elf` and `mm.elf` are gone (targets, `exe_entry.c` files and all); `zelda3d_app` is the only executable and the games ship only as cores. That removed the reason the chooser used to `exec`: with one process, picking Majora's Mask is a HANDOVER. `Context::RequestGameSwitch(id)` records the wanted game in libultraship — the one library the core and the launcher share — the core ends its own game, and `main`'s loop loads that core next. The `_exit(0)` in `DeinitOTR` and the `DeinitOTR(); exit(0)` tail in `graph.c` both went with it: a core must return, and the graph tail additionally had to stop skipping `Main_Shutdown()` (it stopped the audio thread late, which is survivable before process death and not before another game boots on the same engine). **Nothing is torn down between games** — `Context::DestroyInstance()` is still the measured-crashing path (claim C057) and stays the launcher's, once, at exit. EVIDENCE: `tools/zelda3d_sequence.sh oot,mm` (the previously-impossible direction — OoT used to `_exit`) reports both cores returning 0, MM attaching with all five per-game subsystems FRESH, `(none)` inherited, no crashes; and the real chooser path was driven end-to-end with the pid unchanged across the switch (`launcher pick mm` → same pid, exe still `zelda3d`, MM's REPL live). **2026-08-07, second pass — a core is NOT re-runnable, and that is now the blocker.** The switch works because each core runs ONCE; running one twice crashes, and the crash MOVES with each fix because it is a class, not a bug: a core stays dlopen'd, so every global and every file/function-local `static` outlives the run that owned it. Four instances found in a row (`gPlayState`; `graph.c`'s `runFrameContext`, which holds the frame loop's RESUME POINT; a `static` CollisionHeader cache in `GraveHoleJumps.cpp`; OoT audio state, undiagnosed). Fixed by giving the core an owner: `Shipwright/soh/src/zelda3d/core/zelda3d_core_lifecycle.c` DEFINES `gPlayState`/`gGameState` (moved out of `z_play.c`/`game.c`) and `Zelda3D_CoreRunBegin()` resets them plus `runFrameContext` before `InitOTR`, so a run starts clean by construction; `Zelda3D_CoreRunEnd()` reports anything a teardown left set (validated in BOTH directions — it names both pointers when the unwind is reverted). Also fixed a bug that predates all of this: `Graph_ThreadEntry` stopped calling `RunFrame` the instant an exit was requested, abandoning a live gamestate, so **`Play_Destroy` never ran on any quit** — no actor destroy callbacks, no `ZeldaArena_Cleanup`, no `OnPlayDestroy` hooks. (NOT a save flush: there is no save in `Play_Destroy`; that claim appeared here and in two other notes and was wrong.) **2026-08-11 — `oot→mm→oot` now works**: all three cores return 0 and run 3 boots through `Play_Init` into the title scene. Three more instances of the class, each hidden behind the last — `gAudioContext` (note pointers into the `gAudioHeap` that `Heaps_Free`/`Alloc` replace per run), `AudioMgr_Init`'s `hasInitialized` latch (skipped ALL audio engine init on run 2+, which is why the first was never repaired), and the five message tables (rebuilt only `if (ptr == NULL)`, entries are `c_str()` into the departed session's `SOH::Text`) — plus `RunFrame`'s `hasSetupSkybox`, a second run-scoped static three lines below the one the previous pass hoisted out. State goes on `Zelda3D_CoreRunBegin`'s reset list; LATCHES now carry their own run stamp instead (`static Zelda3DOnce x; if (Zelda3D_Once(&x))`) so they need no list — the skybox miss is the case for that. Also fixed: `OTRAudio.h` declared the audio control block `static` IN A HEADER, so `savestates.cpp` locked a different mutex than the audio thread and that exclusion never existed. A per-run leak of 148 strdup'd sequence/soundfont names is closed. **That process-exit crash is FIXED (2026-08-12, issue #9)** — it was one borrowed GPU handle released twice, surfacing far from the release because SDL3 queues released resources; `mm` alone now exits 0. **The ROUND TRIP now works too**: instance 8 was the REPL's own FIFO descriptor (a function-local static, so a second run inherited a descriptor onto the previous run's FIFO and never re-created the path -- it fails as an apparent HANG, not a crash), and instance 9 was `Context::RequestExit`'s flag, cleared in `BeginGameSession` and therefore only when the game NAME changed -- so `oot -> oot` inherited its own latched `quit` and unwound on its first frame while every observable said success. That one is now `Context::BeginRun()`, called by the LAUNCHER before each `core->run()`, which is the one place that knows a run is starting and the same place for both games. Consequently the ESC menu's **"Return to Launcher" row now exists** (`switchgame="oot"`, handled entirely in SohRmlUi so it works from MM with no MM code). Gates: `tools/zelda3d_switch_test.sh` grew to FOUR assertions (pid unchanged / MM reaches a scene / it comes BACK / the ESC row itself works, activated by name via the new REPL `menurow`), and `tools/zelda3d_sequence.sh` now requires every core to ANSWER with a real scene -- it certified `oot,oot` while the second core was unwinding on its first frame, which is how instance 9 survived. See `docs/issues/0016` for the table and the remaining arc. **2026-08-12 — the MM core got the same lifecycle, having had NONE.** `2ship/src/code/main.c` went straight to `InitOTR`: no `CoreRunBegin`, no `CoreRunEnd`, no `Zelda3DOnce`, and it could not share OoT's because the cores are `RTLD_LOCAL`. `tools/zelda3d_sequence.sh mm,mm` (never run before) SIGSEGV'd in `RegisterDebugMode` on the identical `if (gPlayState != NULL)` line. New `2ship/2s2h/zelda3d/mm3d_core_lifecycle.{c,h}` mirrors soh's; MM's `Graph_ThreadEntry` gained `|| gGameState != NULL` **and** its inner frame loop gained `&& WindowIsRunning()` (one without the other converts the crash into a hang), so MM destroys a gamestate for the first time; `gAudioCtx` is zeroed per run and `OTRAudio_Exit` NULLs what it frees; and MM's `GameState_Destroy` now calls `ResourceMgr_ClearSkeletons()` as soh's has for years. `mm,mm` exits 0 with both runs in scene 111. **2026-08-12, sweep pass 3 — the survey's remaining items, worked with three mechanisms rather than one.** STATE goes on the reset list (`GameInteractor::State`'s 20 cheat toggles; `nametag.cpp`'s raw `Actor*` vector plus its now-lying mirror of the hook registry; `ExtraTraps`' armed trap; MM's `CutsceneManager`, whose scene cutscene list is authentically sticky WITHIN a run and dangling ACROSS one). LATCHES that belong to a run carry a run stamp (`Warping.cpp` and `valueViewer.cpp`'s `static bool loadedConfig`, the 3rd and 4th of that conversion -- since the GuiWindow list is per-run, only the FIRST run ever loaded its saved config). OVERLAY state uses `ActorResetFunc`, which fires when the last instance unloads and so matches N64 overlay-reload semantics exactly: `ovl_Fishing`'s ~110 statics (its existing `Fishing_Reset` covered 2), and Boss Dodongo's malloc'd lava textures -- a leak, and a use-after-free if freed without first calling `Gfx_UnregisterBlendedTexture`. That last one generalises: `Interpreter::mMaskedTextures` is engine-lifetime but holds a mask pointer into the departing core and a replacement into its heap or its per-game ResourceManager, so `Context::BeginRun` now clears it and reports the inherited count. See `docs/issues/0016` "Sweep pass 3". **2026-08-12 — both cores now leak NOTHING per run** (claim C081): one run and two runs of the same core give byte-for-byte identical LSAN totals (`oot`/`oot,oot` 29,404,103 B / 6,217 allocs; `mm`/`mm,mm` 28,350,907 / 9,041). The last three causes were MM's `ResourceMgr_ListFiles` strings (the array was freed, the strings never), OoT's `cameraStrings` heap copy, and OoT's CrowdControl/Sail/Anchor — the last held back by a documented guess that `Disable()` might not join, which it does. Attribution goes through **`tools/leak_diff.py`** (instrument I036), which diffs two LSAN reports by allocation SITE rather than by total. Gates: `tools/zelda3d_switch_test.sh` (chooser switch, pid + reaches-a-scene), `tools/zelda3d_sequence.sh <a,b>`, `tools/zelda3d_bootfail_test.sh` (a core must RETURN on a boot failure, not exit the process), and **`tools/zelda3d_deep_check.sh`** -- sanitizer build + dwell across oot,oot / mm,mm / mm,oot,mm. That last one exists because the fast gate quits each core at its first scene and therefore said nothing about the rest: `oot,oot` passed it for weeks while run 2 crashed in `TitleRider::releaseMount`, found only because a sanitizer run is slow enough to stay in the title cs. It refuses to run (exit 2) rather than fall back to the release build, and its verdict states what it does NOT cover. |
| **Embedded-Azahar oracle harness** | ✅ transport + paired gameplay camera verified; ✅ cache-only host title capture | `tools/soh3d_harness/` (`main.cpp` composition plus focused C++ owners, including `paired_camera_control.*` and `framebuffer_snapshot.*`); executable builder `tools/soh3d_harness.py`; public client `tools/harness_cli.py`; cache-only title client `tools/title_host_capture.py`; focused allocator/build/cache/gameplay/headless-display/path/process/ROM-environment/runtime-input/transport owners plus `repo_environment.py`; persistent capture ownership in `harness_cache.py`, `oracle_cache.py`, and `oracle_fragment_summary.py` | `oot3d-decomp/docs/oracle.md`; `oot3d-decomp/docs/gameplay_camera_view_apply.md`; `docs/parity-workflow.md`; `debug_journal/2026-08-30-{boss-fd2-material1-tev-audit,title-unified-cmb-state-contract}.md` | Play/environment/player/actor/input/warp/lighting/camera/animation snapshots and comparisons have focused owners. `soh_snapshot` captures only the host framebuffer and `soh_unified` makes the selected renderer bits explicit. `title_host_capture.py` refuses cache misses before launch, pins texture/raster identity before key construction, advances only SoH naturally, and verifies the live title cursor. Oracle cache identity resolves the same `.env` ROM as the child launcher and includes the effective texture-pack manifest; raw fragments, title frames, and nearest-depth summaries are reusable without another oracle run. |
| **Display-list harness** | 🟡 structure split complete; runtime untested | `Shipwright/libultraship/tools/dlist_harness/`: 101-line `dlist_harness.cpp` composer plus `harness_options`, generic/Zelda3D fixtures, `recording_rendering_api`, `headless_window_backend`, `sdl3gpu_headless_environment`, `ppm_output`, and shared fixture/interpreter-state contracts | — | The dead OpenGL/EGL harness path is deleted and `--gpu` now exercises SDL3GPU. Static/focused tests passed in the implementation task, but the harness was not run against the private ROM/generated-model inputs on the changed tree, so this row does not claim rendering evidence. |
| **Parity/sweep tooling** | ✅ broad base; ✅ MM 12-scene phase tour verified | mature sweep tools plus `mm_phase_tour.py` (CLI), `mm_phase_{session,artifacts,orchestration,catalog,report}.py`, direct `mm_runtime_{paths,manifest,launch,lease,lifecycle,errors,test_fixture}.py` owners, and shared `repo_environment.py` | `docs/parity-workflow.md`; `debug_journal/2026-08-27-boss-fd2-policy-and-mm-phase-tour.md` | The serial MM driver validates shipping phase-report output rather than reimplementing animation logic and scopes the skinned-render opt-in to this diagnostic. The exact 12-scene run reported 24 pairs, 16 MOVED, 8 THIN, 0 STUCK, 0 unmapped, and 316 morph samples. This is bounded coverage, not evidence for default-on or closure of PARTIAL-15. |
| **Kanban / backlog** | ✅ done | `KANBAN.md` (local markdown board — edit directly), `BACKLOG.md` (pointer) | `CLAUDE.md` kanban section | Local board IS the source of truth (edited directly with normal file tools, no round-trip script). USER-DRIVEN work only; sweep-discovered gaps must NOT become cards (fix in-session + journal instead, per hard rule). |
| **N64 OoT decomp integration (`overlays/`, `code/`)** | ✅ vendored, faithful (SoH heritage) | `Shipwright/soh/src/overlays/actors/` (281,169 lines across 885 actor-overlay directories), `code/` (111,501 lines, 152 files) | — | This is the inherited Shipwright/SoH decomp, not SoH3D-authored; treated as the faithful N64 runtime spine that `zelda3d/` intercepts through narrow typed seams. |
| **MM native path — 2S2H glue** | 🔬 early; focused Player form-model + exact animation/base/sheath/hand/material routes landed | `2ship/2s2h/` (`zelda3d/repl/` owners; `mm3d_player_force.{c,h}`; `mm3d_player.c/.h`; `mm3d_player_model*`; `mm3d_player_animation*`; `mm3d_player_mesh_policy*`; `mm3d_player_sheath*`; `mm3d_player_{left,right}_hand*`; `mm3d_player_bottle_material_policy*`; `mm3d_player_deku_spin_material*`; `mm3d_draw.c`; boot/UI glue); shared FIFO framing in `Shipwright/libultraship/include/libultraship/bridge/fifo_rpc.h` | `docs/MM_NATIVE.md`, `docs/MM_SKELANIME_PORT.md`; `mm3d-decomp/docs/player_models.md`; `mm3d-decomp/docs/player_draw.md`; `debug_journal/2026-08-{27-mm-player-{base-mesh-reset,sheath-selector},28-mm-player-left-hand-selector}.md` | Player body selection uses explicit form CMBs, while normal animation comes from the separate 847-CSAB shared GAR through exact form-directory paths. Duplicate basenames cannot alias, Human's child→boy fallback is measured, and unmapped Player resources do not choose a default clip. A typed live tour proved exact per-form submission masks and moving form-specific clips. The exact retail sheath stage adds Deku's sheath and Human sword/sheath/back-shield variants from typed Player/equipment state; a live Human Kokiri/Hero transition submitted `0x370000028` idle, `0x370000020` with authentic R shielding, then restored `0x370000028`. The complete left- and right-hand selectors are isolated behind typed adapters: the right hand retains retail precedence for Zora state/boots, Human held shields, moving hands, animation overrides, Giant's Mask, and Deku drink animations; the left hand selects form-specific open/closed hands, Human swords, bottle/content variants, the exact live-`En_Boom` open-hand lifetime, and Fierce Deity's additional mesh 1 with sword mesh 8. Authentic empty-bottle control changes only the closed-hand/bottle bits, and empty/fish live material writes produce the exact two distinct retail RGBA rows. The recovered Deku-spin stage writes retail's four-word phase-derived alpha to material 6 constant 4 through the same emit-ordered channel. Full retarget framing, Deku normal wait/walk routing, the mount-transition half of the private open-hand flag, retail-only Zora wait/demo identities, live sword/instrument and Deku-spin capture, and transformation movement parity remain open. |
| **MM native path — vendored N64-MM decomp** | ✅ vendored; 🟡 force-hook coverage partial | `2ship/src/`, `2ship/include/` (native Player actions/types), with new control adapters in `2ship/2s2h/zelda3d/mm3d_player_force.{c,h}` | `re_control_debug_backlog.md` MM section; `docs/re-frontier.md` `mm.force-hook-layer` | The established 22-state force layer is runtime-verified; the new Goron/form adapters call shipping entry paths without moving behavior into the 22,000-line overlay. Most numbered action functions remain intentionally unnamed and additional transformation-specific states remain open; identify them per behavior rather than batch-guessing names. |
| **MM3D asset/format decomp + rigid/skinned render** | 🟡 rigid LIVE; skinned gated pending wider tour | `Shipwright/cmb3d/asset/{gar,lzs}.{h,cpp}` (GAR2 parser + LzS inflate), `2ship/2s2h/zelda3d/mm3d_model.cpp` (objectId→GAR2→CMB→draw + SkelAnime hooks), generated `mm3d_animmap.inc` | memory `mm3d-assets-gar2`; `docs/re-frontier.md` `mm3d.gar2-parser` and `mm.skinned-csab` | GAR2/LzS and rigid rendering are live. The corrected MM CSAB parser now measures 1,848/1,945 clips animated with 97 genuinely frozen; the OoT control remains 173/179. The generated map has 1,411 entries under the FULL-132/PARTIAL-15/ZERO-9/excluded-36 policy. Morphing is ported and measured across 3,660 samples (kamome 3,615, dog_bark 45). The honest next step is the still-unrun serial 12-scene phase tour, followed by a decision on PARTIAL-15 coverage and default-on policy; no changed-tree live evidence exists yet. |
| **OoT3D decomp corpus (ground truth)** | ✅ deep, actively growing | `oot3d-decomp/docs/` — 69 docs (actor system, warp, title arc ×26, camera, lighting, cutscene format, player, en_horse, boss_goma, ram_map, static_decomp, divergence_map, ...) | `docs/re-frontier.md` (this pass's new ordering of it) | This is the primary, most mature RE corpus in the whole project. `divergence_map.md` and `state_map.md` are likely the highest-leverage un-consulted docs for future sessions — skim them before starting new RE. |
| **N64 boot/libultra glue (vendored)** | ✅ vendored, untouched | `Shipwright/soh/src/boot/`, `buffers/`, `dmadata/`, `elf_message/`, `libultra/` | — | Inherited Shipwright boot sequence + libultra shim; not SoH3D-specific, no known gap. |
| **libultraship support dirs (vendored/generic)** | ✅ vendored | `Shipwright/libultraship/extern/StormLib/` (MPQ archive lib, unrelated 3rd-party dep), `imgui_shim/` (Dear ImGui integration), `include/` (public headers mirroring `src/ship`+`src/libultraship`), `tests/`, `tools/` (incl. `dlist_harness/`) | — | Generic engine plumbing; not itself a parity surface. |
| **Asset exporter + release packaging** | 🟡 Linux AppImage path locally verified; public artifact pending | `Shipwright/OTRExporter/` (ZAPD-driven OTR/O2R exporter), `launcher_bootstrap/mm_assets.py`, `tools/build_mm_custom_archive.py`, `tools/build_appimage_release.py`, `tools/package_appimage.py`, `packaging/`, remaining platform helpers under `Shipwright/scripts/`, dependency setup under `scripts/` | issue 0024 | The one Linux release path builds with Clang on an Ubuntu 22.04 ABI baseline, regenerates each redistributable port archive with its game-correct exporter, stages the launcher, both cores, extractor metadata, and the SVG icon, then verifies payload completeness, ABI ceilings, core loading, and absence of ROMs plus `oot.o2r`, `oot-mq.o2r`, and `mm.o2r`. The inherited SoH-only CPack/AppImage route is removed. |
| **MM3D scene/room rendering** | ✅ LIVE, default-on (2026-07-21) | `2ship/2s2h/zelda3d/mm3d_draw.c` (`Zelda3D_TryDrawRoom`), `mm3d_model.cpp` (`loadSceneRoom`, `Zelda3D_MM_RoomModelId`), `mm3d_scene_names.inc`, shared parser `Shipwright/cmb3d/asset/{cmb,zsi,lzs}.cpp` | `debug_journal/2026-07-21-mm-scene-room-pipeline.md`, `mm3d-decomp/docs/joker_anchors.md`, test `tools/zelda3d_room_geom_test.cpp` | MM renders **3DS room geometry for its whole world**, no longer N64. Scene table 102/102; MM3D scene ZSIs are LzS-compressed (`ZSI\x09`, `/scenes/`) unlike OoT3D's raw `ZSI\x01` `/scene/`. Two CMB parser bugs fixed, both of which OoT3D hid by coincidence: **index-region offset** (`prm.first` counts u16 SLOTS -> always `first*2`, never `first*elementSize`; OoT3D prms are all USHORT so the two coincide) and **mesh-entry stride** (version-gated 0x04 v6 / 0x0C v10, was hardcoded 4 -> orphaned 27 of 41 sepds and built only 49% of a room). MM3D scene coords are IDENTICAL to N64 (verified by matching the room ZSI actor list against the live N64 actor list) — do NOT add a placement transform. Evidence: **310/313 rooms pass** the structural test (finite, sane extent, zero orphan sepds; the 3 carry no CMB and fall back to the N64 room), plus South/East Clock Town and Termina Field verified live. `ZELDA3D_MM_SCENE` remains only as a debug-isolation knob (0=off, 1=draw, 2=skip-only), mirroring OoT's `ZELDA3D_SCENE`. |
| **MM3D scene collision** | ✅ LIVE, default-on (2026-07-22) | `2ship/2s2h/zelda3d/mm3d_collision.{h,cpp}`, shared parser `Shipwright/cmb3d/asset/zcol.{h,cpp}`, hook in `2ship/2s2h/z_scene_2SH.cpp` (+ mirror in `2ship/src/code/z_scene.c`), pool sizing in `2ship/src/code/z_bgcheck.c` | `debug_journal/2026-07-21-mm-scene-room-pipeline.md`, tests `tools/zelda3d_collision_test.cpp` + `tools/zelda3d_collision_layout.cpp` | MM walks on the SAME geometry it renders. MM3D's collision layout differs from OoT3D's in two version-gated spots (count triplet at +0x1e not +0x1c; `CollisionPoly` omits the +0x06 flags word so the normal starts there), derived via the format's own plane invariant and verified on **110/111 scenes** at 100% plane / 99.9% face-normal. Also required sizing BgCheck's static-lookup node pool from the ACTUAL mesh (`BgCheck_CountStaticLookupNodes`) — the stock per-scene `memSize` budget is tuned to the N64 mesh and the denser MM3D one oversubscribed it 2.4x, hanging scene load. Verified live: Link lands exactly on the 3DS floor, exits fire (walked the Clock Town south gate into Termina Field), and Termina Field loads + walks with ground height tracking. Debug knob `ZELDA3D_MM_COLLISION` (0=off) only. |
| **MM asset extraction/build** | 🟡 partial | `2ship/assets/` (archives, code, interface, misc, objects, overlays, scenes, text, extractor, xml, custom) | `docs/MM_INTEGRATION.md` | Mirrors 2S2H's asset pipeline; extraction path exists, coverage vs the MM3D 3DS assets not separately audited this pass (see MM3D asset/format decomp row for the harder blocker). |
| **Texture pack / hi-res assets** | ✅ direct folder/ZIP loading + transactional toggle | `Shipwright/cmb3d/asset/texpack.{h,cpp}`; `Shipwright/soh/src/zelda3d/texture_pack/texture_pack_runtime.{h,cpp}`; generation-aware model/atlas/HUD caches; REPL `commands/texture_pack.{h,cpp}` | `docs/parity-workflow.md` "Hi-res texture pack"; `docs/oot3d-hd-texture-packs.md`; `debug_journal/2026-09-04-cursor-fps-v3-oot3d-full-build.md` | CMB textures match Citra legacy CityHash64. The packaged runtime discovers a user-owned extracted folder or original ZIP/Zip64 in app data, reads `pack.json`, validates legacy hashes/title ID/limits, and never extracts the archive. Its independent saved switch invalidates CPU model, atlas, HUD, and GPU texture caches as one generation; active OoT3D gameplay commits on a same-entrance `Play_Init`, while Original mode applies immediately. The oracle harness still drives the same pack on both sides via `ZELDA3D_HARNESS_TEXPACK=on\|off`. |
| **OoT Normal/MQ archives + local Randomizer** | ✅ deterministic dual cold provision and 12-MQ live gate pass | `Shipwright/soh/soh/host/rom_auto_extraction.cpp`; `tools/rom_{identity,provision}.py`; deterministic exporter inputs/fields in `Shipwright/ZAPDTR/ZAPD/`; archive loading in `Shipwright/soh/soh/OTRGlobals.cpp`; SoH Randomizer generation bridge | `docs/cursor-fps-v3-oot3d-full-build.md`; `debug_journal/2026-09-04-cursor-fps-v3-oot3d-full-build.md` | Normal and MQ readiness are independent; existing archives are preserved, both missing editions can be generated in one boot, and later boots skip extraction. Two isolated cold gates generated/loaded both owner-derived archives; all 73,878 resource payloads matched between runs. The second enabled OoT3D mode, produced a local seed with all twelve dungeons MQ, and tore down cleanly. ROMs and O2Rs remain outside the public source handoff. |
| **Shadows + AO** | ❌ REMOVED (2026-07-16) — row was STALE | — (the Zelda3D sun-shadow + SSAO code is gone; `zelda3d_sdl3gpu.cpp` keeps only the repurposed ex-shadow sampler slot) | memory `soh3d-title-no-loop-and-effects-removed` | The dynamic sun-shadow and SSAO enhancements were DELETED at user directive ("OoT3D lighting only"); this row still claimed them shipped, which is why #206 first read as a regression in a live system. The N64 per-actor blob shadow (`ActorShadow_Draw`) is separate and still alive — but it draws at the N64 COLLISION floor (`actor->floorHeight`), not the OoT3D render ground, so it is buried under the terrain: that is #206. |
| **RmlUi menu port** | 🟡 partial; structure split complete | `Shipwright/libultraship/src/ship/window/gui/rml/SohRmlUi.{h,cpp}`, focused `Zelda3D{MenuState,LauncherBridge,DiagnosticsBridge,MenuAutomationBridge,RmlUiRegistry}.*`, `Shipwright/libultraship/src/fast/Zelda3DMenuInputBridge.cpp`, and public contracts under `Shipwright/libultraship/include/ship/` | memory `soh3d-rmlui-menu` | Phases 0-1 done (Dusklight-style); Phase 2 input/nav is next. Zelda3D menu state, input, launcher, diagnostics, and registration no longer accumulate in `SohRmlUi.cpp`. |
| **Input scheme (PC-native + hotswap glyphs)** | ✅ done — **v3** (2026-07-28, #203) | `Shipwright/libultraship/src/libultraship/controller/controldevice/controller/mapping/ControllerDefaultMappings.cpp` (the default table), `Shipwright/libultraship/src/ship/controller/controldeck/ControlDeck.cpp` (`kZelda3dInputSchemeVersion` migration), `Shipwright/soh/src/zelda3d/input/zelda3d_keymap.{h,cpp}` (live-binding → HUD label), `gZelda3dInputDevice` | `docs/lus_input_architecture.md`, `debug_journal/2026-07-28-pc-native-keyboard-item-bar.md`, `docs/issues/0002-*`, memory `soh3d-hud-glyphs`, `soh3d-input-scheme` | ESC decoupled from BTN_START (v2); **v3 puts the three C-button item slots on `1`/`2`/`3` and C-Up (look/Navi) on `C`, leaving the arrow keys unbound for the mouse-look pass.** Scheme versioned so existing configs re-migrate. The keyboard HUD badge is composited at runtime from the LIVE binding (was baked artwork that had read "C" while BTN_B was F) — REPL `keycap` prints the four resolved labels, `keycap <label>` dumps the composited RGBA. Gamepad/keyboard glyph hotswap via REPL `inputdev 0\|1`. |

| **Desktop release setup and writable state** | 🟡 implementation/focused tests verified; artifact gate pending | `Shipwright/zelda3d_shared/platform/rom_{identity,install,setup}.*`; `Shipwright/libultraship/src/ship/Context.cpp`; launcher composition in `Shipwright/zelda3d_app/zelda3d_main.cpp`; N64 consumers in `rom_auto_extraction.cpp` and `BenPort.cpp` | issue 0024 | The launcher asks for all four ROMs through native Browse prompts, accepts direct files or exactly one nested ZIP match by content, persists selections transactionally, and activates the same paths both cores consume. Linux writable state follows `XDG_CONFIG_HOME`/`~/.config`; other desktops use their native config location. |

## zelda3d/ reorganization — historical target tree and completed migration

This section records the earlier top-down migration and is superseded by the current structure audit
above. `zelda3d.c` was originally a 7,170-line dumping ground (render +
149-command REPL + input injection + collision + HUD + camera all in one file — banned by
CLAUDE.md's game-structure rule) and 36 source files sat flat at the `zelda3d/` top level with
only `behaviors/{actor,camera,title}` organized into subdirs. The phase table preserves where code
moved at that time; it does not describe current ownership or workflow.

**Historical target tree** (`Shipwright/soh/src/zelda3d/`):

```
core/        lifecycle + global tunables (Zelda3D_Enabled, FrameBegin/FrameEndUpdate, ColdBoot,
             AutoWarp, SceneName) — gathered from zelda3d.c top-of-file
input/       THE ONE input path — consolidates today's 5 scattered pad-mutators + 2 block-checks
             (Zelda3D_InjectKey, Zelda3D_WalkInject, btnhold/walkhold, ZELDA3D_DBG_INPUT)
render/      actor-draw dispatch, room/scene draw, sky/moon/fog/atmos/light, terrain-warp
model/       CMB/CSAB load + skinning (zelda3d_model.cpp, zelda3d_cmab.*, model_internal)
anim/        N64→3DS anim retarget, CSAB resolve (zelda3d_anim.cpp, zelda3d_anim_override.*)
player/      Link draw/pose (zelda3d_link.cpp/.h)
scene/       collision, stairs, lighting/fog table consumers (zelda3d_collision.h,
             zelda3d_stairs.*)
cutscene/    zelda3d_cutscene.cpp/.h + oot3d opcodes header
repl/        the REPL interpreter (~2,270 lines / 149 commands, extracted from zelda3d.c)
hud/         zelda3d_hud_tex.cpp — crisp HUD textures for the NATIVE (N64) HUD
behaviors/   EXISTS — actor/camera/title OOP registry, unaffected by this reorg
tables/      .inc data tables, split by concern (anim/player/scene/model) — Phase 0 landed FLAT
assets/      generated *_png.h texture blobs (527 KB) — Phase 0 DONE
```

**Current structure audit (2026-08-24).** The 1,200-line ceiling and one-responsibility rule are
independent gates. Historical phase rows below record where files moved; they do not override this
current compliance table.

| Surface | Current status | Required next step |
|---|---|---|
| Harness entry point | ✅ compliant | `main.cpp` is 317 lines and composes focused modules; typed SoH state and comparison owners remain separate. |
| OoT REPL | ✅ structure-compliant | The 105-line dispatcher only parses/gates and routes to focused command owners; transport, response, FPS, defaults, menu sync, time, diagnostics, and camera state are separate. |
| BossFd | ✅ structure-compliant; runtime parity open | Focused render orchestrator plus authored-flight, control, effect, profile, and history owners. |
| BossFd2 | ✅ structure-compliant; runtime parity open | `boss_fd2.cpp` composes the hole-form draw and authored CSAB controller; `boss_fd2_mane.{c,h}` owns the ten-point procedural chain and CMB segment submission extracted from the legacy overlay; `boss_fd2_animation_policy.{h,cpp}` owns the exact recovered persistent-action→initial-CSAB mapping and rejects unknown actions instead of inventing a fallback; `boss_fd2_materials.{h,cpp}` owns CMAB resource binding/application while `boss_fd2_material_controller.h` owns the actor/event-local 30:20 clocks (body/hair step 2 per authored draw, exposed-face pulse step 1). Both BossFd mane paths use the focused force-unlit transformed-model option for the authored `vertex_lighting=0` fire-hair material. The private CSAB controller exposes only an opaque diagnostic checkpoint to the paired harness; the production controller remains unchanged unless that explicit control is invoked. |
| Camera at-default | ✅ structure-compliant; runtime parity open | Focused 95-line implementation plus 24-line interface; retain narrow decomp seams. |
| MM external control/phase tooling | ✅ focused modules; 12-scene runtime tour verified | Python transport/process/runtime/catalog/report/tour owners are separate; exact process identities and phase-report policy are regression-tested. |
| MM in-game REPL | ✅ structure-compliant | `zelda3d/repl/` separates composition, transport, framing, lifecycle, parsing/routing, world, scene, model, and Link responsibilities; keep the old root files retired. |
| Animation | ✅ structure-compliant | `zelda3d_anim.cpp` is 650 lines; automatic playback, pose evaluation, inspection, tracking, submission, and the skeleton draw bridge have focused owners. |
| Former `behaviors/actor/actor_overrides.{h,cpp}` | ✅ grab-bag retired | En_Ge1 lives in `gerudo_white.*`, En_Ko in `kokiri_kid.*`, En_Hy in `townsfolk.*`, and En_Niw/procedural replay in `cucco_wing_override.*`; the mixed file is deleted. |
| SoH core/player | ✅ structure-compliant | `core/zelda3d.c` (40 lines) and `player/zelda3d_link.cpp` (37 lines) compose focused runtime/control/scene/diagnostic and draw/retarget/pose/REPL owners. |
| SoH renderer | ✅ grab-bag retired | The former zelda3d_render header/source pair is deleted. Actor dispatch/observation/replacement, model submission/draw, room/sky/celestial/fog/lighting, terrain, camera, lifecycle, and time have narrow owners under `Shipwright/soh/src/zelda3d/render/`. |
| Fast renderer API | ✅ focused contracts | The former zelda3d_gl umbrella is deleted. Nine focused owners under `Shipwright/libultraship/include/fast/` cover model types/provider, submission, pose, material overrides, lighting, fog, instrumentation, and render control; frame capture, SDL, and UBO remain separate. Native group isolation is owned by the instrumentation/pass seam: `sgdrawlist` reports model-local group and CMB material identities, while default-inert `sgdrawonly`, `sgdrawskip`, and `sgdrawskipafter` compose with the stable model filter. The selected `FRAGDBG_DRAW` UBO gate preserves neighboring pass context for per-draw TEV taps. The embedded harness exposes the same host controls without re-owning renderer policy. |
| Engine compatibility API | ✅ focused contracts | The former functions umbrella is deleted; responsibility headers live in `Shipwright/soh/include/functions/`, and new declarations go to their owner. |
| RmlUi/randomizer host bridges | ✅ extracted below ceiling | `SohRmlUi.cpp` is 1,150 lines and `randomizer.cpp` 1,190; Zelda3D menu state/input/launcher/diagnostics/registry and randomizer actor-check/generation lifecycle/policy live in focused bridge modules. |
| Selected cohesive legacy files | 🟡 frozen/no-growth | `model/zelda3d_model.cpp` remains above 1,200 and is ratcheted. Oversized generated/vendored decomp owners stay frozen until a real responsibility boundary is identified; caps are never raised. |
| Python source-size enforcement | ✅ enabled | The verifier includes first-party Python. `gen_mm_animmap.py` is a 195-line composer over archive, inventory, matching, types, defaults, C-table emission, coverage, report, paths, and verification owners. |

**Historical migration status** (a “Phase done” marker means the named move completed, not that the
result satisfies today's ownership/size gates). Present-tense wording such as “now” and every line
count inside these cells are snapshots from that migration phase; the current audit and generated
tree above supersede them:

| Module | Status | Notes |
|---|---|---|
| `assets/` | ✅ Phase 0 done | 8 `*_png.h` blobs (button_tex, counter_icon, digit_tex, heart_tex, key_glyphs, num_glyphs, stairs_stone, xbox_glyphs) moved via `git mv`; all 8 `#include` sites updated (`zelda3d_hud_tex.cpp` ×7, `zelda3d_stairs.cpp` ×1). `key_glyphs_png.h` replaced the old `kbd_glyphs_png.h` in 2026-07-28 (#203): the four per-slot baked key pictures became a blank keycap + a monospaced glyph atlas the HUD composites at runtime, generated by `tools/zelda3d_gen_key_glyphs.sh` from `Shipwright/soh/assets/zelda3d/key_cap.svg` + the repo-vendored Inconsolata (a system font would make the output machine-dependent). |
| `tables/` | ✅ Phase 0 done | 7 `.inc` files moved via `git mv`, kept FLAT (not split into `tables/{anim,player,scene,model}/` — flat chosen over the plan's optional per-concern split for simplicity, since consumers are already few and named per-concern in the filename itself). All 7 `#include` sites updated across `zelda3d.c` (×5), `zelda3d_anim.cpp` (×1), `zelda3d_link.cpp` (×1). |
| `input/` | ✅ Phase 1 done; +`zelda3d_keymap` (#203) | **`zelda3d_keymap.{h,cpp}`** (2026-07-28) is the read-only view of the live bindings: `Zelda3D_KeyLabelForButton(bitmask)` → the keycap label for whichever keyboard key is bound to an N64 button, so the HUD can never disagree with the input layer. Tie-break when several keys are bound is the LOWEST SCANCODE — `ControllerButton::GetAllButtonMappings()` is an unordered_map, so without one the badge flickers. PC-native **input scheme v3**: the three C-button item slots are `1`/`2`/`3` and C-Up (look/Navi) is `C`; the arrow keys are unbound, reserved for the mouse-look pass. Bump `kZelda3dInputSchemeVersion` in `ship/controller/controldeck/ControlDeck.cpp` whenever the keyboard default table changes or existing configs will not re-migrate. Phase 1: `zelda3d_input.h`/`.cpp` landed: `Zelda3D_InjectKey` (was orphaned in `zelda3d_model.cpp`), `Zelda3D_WalkInject` + `walkhold`/`btnhold` globals + their REPL handler bodies, `Zelda3D_XboxBtnEnabled`/`Zelda3D_InputDevice`, and a consolidated `Zelda3D_DbgInputEnabled()` (was a private static lambda duplicated in both `Shipwright/libultraship/src/{ship,libultraship}/controller/controldeck/ControlDeck.cpp`). Block-decision is a SINGLE source of truth: `Ship::ControlDeck::AllGameInputBlocked()` (`mGameInputBlockers` map, set only by `SohRmlUi::SetVisible`) — `KeyboardGameInputBlocked()`/`GamepadGameInputBlocked()`/`MouseGameInputBlocked()` all delegate to it (the ImGui-stub false-positive that used to make this two independently-wrong checks was already fixed same-day in 65acc6c5/583ecfb0, before this phase). Poll-time diagnostic added in `KeyboardKeyToButtonMapping::UpdatePad` (per-scancode, log-on-change) so `ZELDA3D_DBG_INPUT=1` now shows whether a latched keypress survives to the pad-fill poll, not just whether the SDL event arrived. See `debug_journal/2026-07-15-phase1-input-consolidation.md` for the full root-cause trail on the "title/Play keyboard dead, file-select works" report — not resolved by static analysis alone, needs the extended headed diagnostic to pin down. |
| `core/` | 🟡 Phase 2b step 3 partial | `zelda3d_math.cpp`/`.h` landed: `Zelda3D_ActorTurnToPoint`/`PathFollowUpdate`/`ActorMoveXZByYawSpeed` (ported OoT3D locomotion primitives, called externally by `behaviors/title/title_rider.cpp`'s `TitleRider::step()` via its own pre-existing local forward decls, unchanged by the move) + the still-dead standalone `Zelda3D_RiderReset`/`RiderStep` integrator and its private state, self-contained (one contiguous 176-line block, no entanglement). The title-cam/light-slot half of step 3 was NOT done as a separate move: `Zelda3D_TitleLightSlotsConvert`/`Slots`/`Count` turned out entangled with `Zelda3D_UpdateLight` and were already pulled into the former zelda3d_render implementation in step 1 (see that row). `Zelda3D_TitleCamEnabled`/`Zelda3D_TitleLightSettingsOverride` (both tiny thin wrappers, `behaviors/title/title_presentation.cpp` + `z_kankyo.c` call sites) were deliberately LEFT in `zelda3d.c` — trivial, no meaningful entanglement, low value to move relative to session cost; a future session can fold them into `core/` or `behaviors/title/` alongside a real "lifecycle + tunables" `core/` sweep (`Zelda3D_Enabled`/`FrameBegin`/`FrameEndUpdate`/`ColdBoot`/`AutoWarp`/`SceneName`, per the target-tree description above) if one is ever done. `zelda3d.c` is now ~1,770 lines (from 6,385 pre-Phase-2b). Verified via grep-based symbol audit + brace-balance — no build available this session. |
| `render/` | ✅ Phase 2b step 1 done | `zelda3d_render.cpp` (~1,940 lines) + `zelda3d_render.h` landed: actor-draw dispatch (`Zelda3D_TryDrawActor`/`TryAuto`/`EmitModelDraw`/`DrawModelGL`/`DrawActorModel`/`EmitActorBillboard`), room/scene draw (`DrawRoomGL`/`TryDrawRoom`), sky/moon/fog/atmosphere/light (`TryDrawSky`/`TryDrawSunMoon`/`TryDrawTitleAtmos`/`WorldShadeBlend`/`UpdateLight` + the sky/moon model-id helpers), terrain-warp Y-offset (`RenderYOffsetAtXZ`/`ActorRenderYOffset`/`TerrainWarpEnabled`), plus the title-cs light-slot converter and cutscene-cam reconcile (`ReconcileCutsceneCam`) that turned out entangled with `UpdateLight`. The 29-symbol shared-statics list (`sModelTable`, `sAuto`, `Zelda3D_AutoMode`, `Zelda3D_ActiveSkyIndex`, `Zelda3D_UpdateLight`, `Zelda3D_ReconcileCutsceneCam`, `Zelda3D_ActorObjectId`, `Zelda3D_FogSetPosition`, `Zelda3D_InitForceTime`, `Zelda3D_N64FloorCb`, `Zelda3D_SkyModelId`, the generic actor-pin/selection-draw/motion-sample/aim statics, `gZelda3dEnKoMaskOverride{,Set}`, `sWarpPlay`) is now non-static and declared in `zelda3d_render.h`; zelda3d.c includes it. Two dependency clusters the original scan didn't count also turned out entangled and were pulled in: the `gZelda3dPending{Actor,Model,Scale,GroundOff,Auto,BoneMap,AnimOtr,N64CurFrame,N64AnimLength,MorphWeight}` deferred N64-anim draw handoff (bidirectional between `Zelda3D_TryAuto`/`TryDrawActor` here and `Zelda3D_DoRetarget`/`SkelAnimeDraw`/`SkelAnimeDrawRaw`/`AfterActorDraw`/`SetCurAnim`, which stayed in zelda3d.c — their definitions there are now non-`static` instead of moving) and `sPendingMeasureKey`/`Zelda3D_EmitMeasure` (write side here, read side in `Zelda3D_AfterActorDraw`). `tables/zelda3d_bonemap.inc`'s `Zelda3DBoneMap` typedef gained a struct tag (`typedef struct Zelda3DBoneMap {...}`) so `zelda3d_render.h` can forward-declare it for the `gZelda3dPendingBoneMap` extern without needing the whole per-character table visible. `zelda3d.c` is now ~4,520 lines (from 6,385). Verified by grep-based symbol audit (single definition, matching linkage at every declaration/definition pair, brace-balance per file) — no build available this session. Commit d05e2f75. |
| `repl/` | ✅ responsibility split complete; live command parity unchanged | `zelda3d_repl.cpp` (105-line parser/router), `zelda3d_repl.h`, resettable runtime state, and focused responsibility-named command owners | The former 3,420-line file has become a composition-only router. Command semantics live in domain owners under `Shipwright/soh/src/zelda3d/repl/commands/`; FIFO lifecycle and runtime state remain separate. This structural result does not add runtime evidence. |
| `hud/` | ✅ OFF the Fast3D interpreter (#205 complete, 2026-07-28) | **`zelda3d_hud.{h,cpp}`** (2026-07-28) is the record/draw seam that takes the in-game HUD off the interpreter: the N64 HUD code keeps its LAYOUT (same rects, alphas, cosmetic CVars) and only its final texrect emission becomes a `Zelda3D_HudQuad*` record, drawn in `Gui::EndFrame` through the restored SDL3-GPU quad renderer (`libultraship/src/fast/zelda3d_hud_sdl3gpu.cpp`, extended with a per-vertex ENV colour + combine mode so the heart `(PRIM-ENV)*TEXEL0+ENV` case works alongside plain modulate). **Every element is native:** item buttons (disc + icon + ammo + key badge), the do-action/A button, the heart row, the magic meter, the rupee + small-key counters, the event timer, the HBA score, C-Up/Navi and the minimap image. The reported black-bar corruption recurred on THREE elements (item discs, A button, C-Up) — all the same shared-resident-tile + `bgScale` coupling, all removed by the conversion (`docs/issues/0003-0004`). The do-action pair are ortho-MATRIX quads, not texrects, so their rect is derived from the HUD ortho mapping (`centre = (x+23, y+23)`; verified native-vs-vanilla to 1.3 virtual units) and the flip animation is reproduced exactly as a `cos` vertical scale. The label is IA4, decoded in the module with a CONTENT-HASH cache (`doActionSegment` is rewritten in place, so a pointer-keyed cache would pin the first label) — elements convert as a GROUP because the native pass runs after the whole interpreter frame, so a half-converted element inverts its own layering; `Zelda3D_HudOwns()` is the gate. Deliberately still interpreter-drawn: the minimap's compass/position arrows and map-mark icons — `gCompassArrowDL` is untextured 3D geometry under scale+RotateX+RotateY, so it needs a MESH path not a quad path, and a rotated sprite would be a clone standing in for the real mechanism. They layer correctly over the native map thanks to the flush marker, so this is a scope boundary, not visual debt. **`G_ZELDA3D_HUDFLUSH` + `gSPZelda3DHudFlush`** (built on the `G_ZELDA3D_CLEARDEPTH` precedent) composites an element's quads at ITS point of interpreter execution, which is what lets elements convert individually — a record-time flush would instead BURY the HUD, since recording happens at display-list BUILD time and the interpreter appends its own ops only later (`docs/issues/0005-*`). Diagnostics: REPL `nativehud 0|1` (same-frame A/B — the reason a silently-no-op marker was caught rather than misdiagnosed as a broken compass arrow), `navicall 0|1`. Gotcha: SoH HUD textures are OTR PATH STRINGS that the interpreter resolves at `gDPLoadTextureBlock`; the native path resolves them itself in `record()` (`ResourceMgr_OTRSigCheck` + `..._GetResourceDataByNameHandlingMQ`) — the first native icon draw was blank without it. See `debug_journal/2026-07-28-hud-off-the-interpreter.md`. Also holds `zelda3d_hud_tex.cpp` — crisp runtime-built textures (hearts, digits, counter icons, button disc, keyboard/gamepad glyphs) that the NATIVE N64 Fast3D HUD (`z_parameter.c` `Interface_Draw`, `z_lifemeter.c` `HealthMeter_Draw`) draws in place of the blocky 16x16 originals, gated by `Zelda3D_HudTexEnabled()`. The custom PC/SDL3-GPU HUD (its soh-side layout module, its libultraship-side quad renderer, `Zelda3D_PcHudEnabled/HudUpdateFrame/HudFrame`, the `Zelda3D_Hud_*` C-ABI, the `Op::DRAW_HUD` op class) and the bottom-centre 6-slot item hotbar that suppressed the N64 C-button cluster were DELETED at user request — the native HUD is the only in-game HUD. The keyboard key badge is no longer baked artwork: `Zelda3D_KeyCapTex(label)` composites a keycap from the LIVE binding via `input/zelda3d_keymap.cpp` (#203, 2026-07-28), widening the cap by horizontal 9-slice for multi-character labels — so a rebind in the input editor changes the HUD immediately. Remaining keyboard-UX work is a keyboard-first bindings PAGE in the RmlUi menu (belongs with RmlUi Phase 2 input/nav, not started). |
| `scene/` (collision extraction) | ✅ Phase 2 done | `zelda3d_collision.cpp` landed: `Zelda3D_BuildSceneCollision` + helpers (`N64FloorData0`, `N64WallData0`, `WallSharesEdge`, `PropagateWallClimbBits`, `BaseFloorPoly`, `CollisionEnabled`) moved out of `zelda3d.c`, paired with the pre-existing `zelda3d_collision.h` C-ABI bridge. `Zelda3D_SceneName` un-static'd + exposed via `zelda3d.h` (the new file needs it). Deliberately LEFT `Zelda3D_N64FloorCb`/`sWarpPlay` in `zelda3d.c` — they're shared with the terrain-warp actor-render code elsewhere in the file (same Phase 2b tangle as `repl/`/`render/`), not collision-specific. Commit c688b7d2. |
| `model/` | ✅ Phase 3 done | `zelda3d_model.cpp`, `zelda3d_cmab.cpp/.h`, `zelda3d_overlay2d.cpp/.h`, `zelda3d_model_internal.h` moved via `git mv`; every relative include across `zelda3d/` (and stale comment-paths in `mm3d_model.cpp`) fixed to match. |
| `anim/` | ✅ Phase 3 done | `zelda3d_anim.cpp`, `zelda3d_anim_override.cpp/.h` moved via `git mv`. |
| `player/` | ✅ Phase 3 done | `zelda3d_link.cpp/.h`, `link_mesh_id_map.md` moved via `git mv`; `zelda3d_shared/player/link_midmask.h`'s doc-path comment updated to match. |
| `cutscene/` | ✅ Phase 3 done | `zelda3d_cutscene.cpp/.h`, `zelda3d_cutscene_oot3d_opcodes.h` moved via `git mv`. |
| `scene/` (stairs + collision header) | ✅ Phase 3 done | `zelda3d_stairs.cpp/.h`, `zelda3d_collision.h` moved next to the pre-existing `scene/zelda3d_collision.cpp`. |
| `core/` (zelda3d.c itself) | ✅ Phase 3 done | `zelda3d.c` moved to `core/zelda3d.c` (last loose implementation file). The top-level `zelda3d.h` now remains only as a declaration-free compatibility umbrella for legacy call sites; new code includes focused owner headers directly. `core/zelda3d.c` was **1,417 lines** at this historical phase (down from the original 7,170-line dumping ground; was ~1,946 after Phase 2b's repl/ step) after this phase also pulled the per-actor override glue below out of it. |
| Former `behaviors/actor/actor_overrides.{h,cpp}` | ✅ historical move superseded by responsibility split | Phase 3 first removed these functions from larger monoliths but collected unrelated actor policies. The 2026-08-24 hard-rule audit rejected the former “few fixed call sites” justification: dispatch frequency does not create cohesive ownership. The grab-bag is now deleted; En_Ge1 animation/joints live in `gerudo_white.*`, En_Ko selection in `kokiri_kid.*`, En_Hy selection in `townsfolk.*`, and En_Niw/procedural replay in `cucco_wing_override.*`. |

**Phase 3 also fixed the accumulated Phase-2b build breakage** (the reorg's single end-to-end build,
run once at the very end per the stated plan) — see commit `1689dc30` for the full list: broken
relative includes after these moves, a hardcoded path in the since-deleted charcompare tool's
CMakeLists, ~25
REPL-tunable globals + the `ZELDA3D_GSCALE` macro + `Play_Update` missing from
the former zelda3d_render header's documented shared-symbol contract, a `char*`/`const char*` mismatch,
missing `extern "C"` declarations for symbols crossing the C/C++ boundary, and a `goto` that jumped
over an initialized local (legal in C, ill-formed once that code became a `.cpp` TU in Phase 2b).
The OoT core (`libsoh_core.so`) builds and links cleanly as of this commit.

## Where is X? (direct index)

| Looking for... | Go to |
|---|---|
| Title camera math | `Shipwright/soh/src/zelda3d/behaviors/title/title_camera.cpp`; ground truth `oot3d-decomp/docs/title_camera_lead.md`, `title_camera_containing_struct.md`, `title_view_matrix_lh.md` |
| Title cs dispatch / driver | `title_presentation.cpp` (`Zelda3D_Title_Update`, composition), with activity/camera/rider/atmosphere/lighting/overlay in focused title owners; ground truth `oot3d-decomp/docs/title_gamestate_driver.md`, `title_gamestate_v2.md`, `title_rider_cs_dispatch.md` |
| Title rider (mounted Epona intro) | `behaviors/title/title_rider.cpp/.h`; `oot3d-decomp/docs/title_rider_driver.md`, `title_rider_port_spec.md` |
| Link draw hook | `Shipwright/soh/src/zelda3d/player/zelda3d_link.cpp` (`Zelda3D_TryDrawPlayer`, composition); implementation in `Shipwright/soh/src/zelda3d/player/player_draw.cpp` and `player_draw_policy.cpp` |
| Link facial animation (eye/mouth) | `Shipwright/soh/src/zelda3d/player/zelda3d_link_face.cpp` (`Zelda3D_LinkFaceUpdate`); format `Shipwright/cmb3d/asset/faceb.{h,cpp}`; RE in `docs/re-frontier.md` `player.facial-anim` + `debug_journal/2026-07-23-201d-link-facial-anim-faceb.md` |
| Force-state layer (Link) | `Shipwright/soh/src/overlays/actors/ovl_player_actor/z_player.c` `Zelda3D_PlayerForce*` hooks (search that name); catalog of gaps in `docs/re_control_debug_backlog.md` |
| Oracle transport (the only one) | focused owners under `tools/soh3d_harness/`; `main.cpp` only composes libretro, lockstep, state/probes, comparisons, capture, REPL, watchdog, and process lifetime. `harness_transport.py` owns the build-aware REPL boot deadline; cache-owned PICA command-list provenance, writer, and submitter probes live in `tools/pica_command_{provenance,writer,submitter}_oracle_probe.py`; title-cursor raw command-list and register capture live with title restart/checkpoint policy in `tools/title_oracle_probe.py`; the shared pure packet decoder is `tools/pica_command_list.py`. The provenance probe captures one grounded draw's command buffer, the writer probe owns the rotating-buffer writer-lifetime discriminator plus exact template and active-state input write snapshots, and the submitter probe joins the exact GSP list submission to VM/direct-pointer/bulk-copy provenance with a reversible `WriteBlock` positive control owned by `harness_memory`. |
| Fog / lighting port | `Shipwright/soh/src/zelda3d/tables/zelda3d_scene_lighting.inc`; RE in `oot3d-decomp/docs/scene_lighting.md`, `env_context_layout.md` |
| Object→ZAR replacement tables | `Shipwright/soh/src/zelda3d/tables/zelda3d_object_zars.inc` (generated by `tools/gen_object_zars.py`); coverage in `COVERAGE.md` |
| Actor behavior registry | `Shipwright/soh/src/zelda3d/behaviors/actor_behavior.h/.cpp` (`findActorBehavior`); fixed-call actor owners: `gerudo_white.*`, `kokiri_kid.*`, `townsfolk.*`, `cucco_wing_override.*` |
| Flying Volvagia authored history / comparator | Shipping `Shipwright/soh/src/zelda3d/behaviors/actor/boss_fd.{h,cpp}` plus focused BossFd submodules (`authored_flight`, `steering_math`, forced profiles, history, effects); exact OoT3D trig metadata is generated by `tools/gen_oot3d_trig_table.py`; harness `tools/soh3d_harness/boss_fd_compare.{h,cpp}` + `soh_boss_fd_state.cpp` |
| Hole-form Volvagia animation/render | Shipping `Shipwright/soh/src/zelda3d/behaviors/actor/boss_fd2.{h,cpp}` composes the recovered controller and multipart draw; `boss_fd2_mane.{c,h}` owns the ten-point solver/native-or-CMB segment submission; exact initial clip selection/refusal policy lives in `boss_fd2_animation_policy.{h,cpp}`; paired oracle control/comparison and rendered-anchor camera live in `tools/soh3d_harness/boss_fd_{oracle,control,compare}.*`, `soh_boss_fd_state.*`, and `paired_camera_control.*`; ground truth is `oot3d-decomp/docs/boss_fd2.md` |
| Camera behavior registry / at-default Y-bias | `Shipwright/soh/src/zelda3d/behaviors/camera_behavior.h/.cpp`; `behaviors/camera/at_default.{h,cpp}` |
| N64→3DS anim retarget | `Shipwright/soh/src/zelda3d/anim/{zelda3d_anim,automatic_playback,pose_evaluation,pose_inspection,pose_tracking}.*`, `tables/zelda3d_animmap.inc`, `tables/zelda3d_bonemap.inc`; memory `soh3d-n64anim-retarget` |
| MM player form-model draw / focused controls | Form-specific MM3D body ownership: `2ship/2s2h/zelda3d/mm3d_player.c/.h`, `mm3d_player_model.{cpp,h}`, `mm3d_player_model_policy.{cpp,h}`; retail base mesh-ID reset: `mm3d_player_mesh_policy.{cpp,h}`; sheath/back-shield selection: `mm3d_player_sheath_policy.{cpp,h}` + `mm3d_player_sheath.{cpp,h}`; right-hand/held-equipment selection: `mm3d_player_right_hand_policy.{cpp,h}` + `mm3d_player_right_hand.{cpp,h}`; left-hand/sword/bottle selection: `mm3d_player_left_hand_policy.{cpp,h}` + `mm3d_player_left_hand.{cpp,h}`; bottle-hand and Deku-spin constant-colour selection: `mm3d_player_bottle_material_policy.{cpp,h}` and `mm3d_player_deku_spin_material*.{cpp,h}` (ground truth `mm3d-decomp/docs/player_draw.md`, asset diagnostic `tools/mm_player_cmb_dump.py`, focused gates `tools/test_mm3d_player_{left,right}_hand.py`, `tools/test_mm3d_player_deku_spin_material.py`, and `tools/test_mm3d_player_contracts.py`); exact shared-GAR animation routing: `mm3d_player_animation.{cpp,h}` + `mm3d_player_animation_policy.{cpp,h}`; form-bound phase/morph state: `mm3d_animation_playhead.h`; explicit archive/CMB resolution: `mm3d_model_catalog.{cpp,h}`; Player LOD draw seam: `2ship/src/code/z_skelanime.c`; real action/form/item entry adapters: `mm3d_player_force.{c,h}` |
| MM REPL / control transport | focused in-game owners under `2ship/2s2h/zelda3d/repl/`, typed mutation owner `zelda3d/mm3d_player_force.{c,h}` (real `linkform`, `linkequip`, and `linkitem` entry paths), shared `Shipwright/libultraship/include/libultraship/bridge/fifo_rpc.h`, direct `tools/mm_runtime_*.py` owners and `mm_control.py`; phase gate composed under `tools/mm_phase_*.py` |
| Cross-registry project information query | `tools/info.py`, a repo-relative link to the canonical `../shared/re-harness/tools/info.py`; project facts remain in this repository's goals/state/issues/codemap/frontier/claim/instrument authorities |
| Kanban / backlog | `KANBAN.md` (local board, source of truth), `BACKLOG.md` (pointer) |
| Sweep-discovered non-user gaps | `debug_journal/` (dated entries) — NOT kanban, per CLAUDE.md hard rule |

## Governance

`tools/codemap.py check` cross-checks this table against the real tree (flags subsystems present
on disk but unmapped here, and stale `Where` paths). Run it bare from the repo root after any
structural change before committing. It audits against the **git-tracked** source set, so it
ignores gitignored trees (the vendored `Azahar/` oracle, `build-*/`, `logs/`, `scratch/`) and
does not descend into submodules (`oot3d-decomp/`, `mm3d-decomp/`, `ZAPDTR/`, `StormLib/`) — only
the project's own committed code is checked. It understands the map's path shorthands: a
subsystem-relative `Where` path (`behaviors/camera/normal1.cpp`) resolves against the full tracked
tree, and the "stem-dot-ext-slash-dot-ext" sibling-file shorthand (e.g. `zelda3d_cmab.cpp/.h`, one
token asserting both `.cpp` and `.h`) is expanded and each part verified. A subsystem dir counts as "mentioned" if its path appears anywhere in this doc's
prose (so brace-glob notation like `cmb3d/asset/{gar,lzs}.{h,cpp}` still covers `cmb3d/`). **Not
yet wired into a pre-commit hook this pass** — no `.pre-commit-config.yaml` or
equivalent exists in this repo currently; wiring it is a reasonable next step but was left undone
to keep this iteration doc-only per the task scope. `tools/re_frontier.py check` is the RE-side
equivalent — see `docs/re-frontier.md`.
